# Decision Log

## Status Vocabulary

- Accepted product direction: explicit user choice; implementation details remain flexible.
- Proposed technical baseline: recommended but must pass a stated validation gate.
- Deferred: deliberately postponed; no implementation commitment.
- Superseded: retained for history; do not apply to new worlds.

## Entries

| ID | Date | Status | Decision | Revisit Condition |
|---|---|---|---|---|
| D-001 | 2026-08-03 | Accepted product direction | Continuous bounded 2D continent | Product direction changes |
| D-002 | 2026-08-03 | Accepted product direction | Pixel art with scientific overlay | Observer research changes UX needs |
| D-003 | 2026-08-03 | Accepted product direction | Emergent diet/predation and sexual lineage in evolution release | Model evidence shows harmful ambiguity |
| D-004 | 2026-08-03 | Accepted product direction | Sandbox controls, replayable seeds/configs, data export | Security/experience scope changes |
| D-005 | 2026-08-03 | Accepted product direction | Private continuous operation; servernode3 proposed; no initial GPU passthrough | Live audit or benchmark disagrees |
| D-006 | 2026-08-03 | Accepted product direction | Rules are versioned adjustable guidelines, not fixed doctrine | Never closed; governs future changes |
| D-007 | 2026-08-03 | Proposed technical baseline | Rust SoA kernel plus TypeScript/PixiJS observer | Phase 0 prototype/benchmark |
| D-008 | 2026-08-03 | Proposed technical baseline | Binary WebSocket viewport deltas and versioned local snapshots | Protocol/save spike |
| D-009 | 2026-08-03 | Proposed technical baseline | One isolated VM on servernode3 | Live capacity, backup, and workload audit |
| D-010 | 2026-08-03 | Proposed technical baseline | Local Phase 0 evidence supports retaining Rust, strict deterministic ordering, bounded snapshot framing, and PixiJS for further evaluation; no affected ADR is accepted yet | Deployment-shaped VM, compressed codec, and physical target-browser evidence |
| D-011 | 2026-08-03 | Proposed technical baseline | Phase 1 kernel is all-integer fixed point (positions 1/1024 m, energy/biomass milli-units, Q16 fractions) under versioned policy `phase1-behavior-v1`, with exact ledger conservation, `lifesim-rng-v1` named streams, and `lifesim-worldgen-v1` generation; behavior/RNG/worldgen versions are folded into the config hash | Phase 2 controller work shows fixed point is too restrictive, or cross-platform float policy is decided |
| D-012 | 2026-08-03 | Proposed technical baseline | Phase 1 capacity ceiling rejects births deterministically at `max_entities` (default 5,000) and also applies the reproduction cooldown to rejected attempts; extinction is a latched single event and extinct worlds stay valid | Long-run evidence shows the ceiling or cooldown policy distorts experiments |
| D-013 | 2026-08-04 | Proposed technical baseline | Phase 2 numeric policy (ADR-0011): bounded f32 genome/controller values, rational tanh approximation without libm, `uniform-bounded-v1` variation, integer BAM trigonometry; same-build/same-platform replay guarantee only | Cross-platform requirement, SIMD/batching work, or degenerate-ecology evidence |
| D-014 | 2026-08-04 | Proposed technical baseline | Phase 2 config section hashed only when enabled so disabled configs replay bit-identically to Phase 1 and preserve its fixtures; enabling Phase 2 starts a new replay lineage (`phase2-behavior-v1`) | A future config-file format needs a different compatibility scheme |
| D-015 | 2026-08-04 | Proposed technical baseline | Pairing policy: greedy stable-ID-order selection with (distance, ID) tie-breaks, mutual intent, trait-distance compatibility gate, per-parent genome-derived investment split (lower ID pays the rounding remainder), cooldown applied on rejection too; controller output mapping gives baseline mobility with rest opt-out and permissive eat/mate thresholds | Long-run evidence shows the pairing or mapping policy distorts selection dynamics |
| D-016 | 2026-08-04 | Proposed technical baseline | Phase 3 server stack: threaded `tiny_http` + `tungstenite` (no async runtime), loopback-only bind, tick thread isolated from client I/O by bounded latest-wins queues with keyframe-collapse backpressure; the two justified new dependencies are recorded here | Observer counts or transport needs outgrow the threaded model |
| D-017 | 2026-08-04 | Proposed technical baseline | Private auth boundary: bearer tokens from env or startup-generated OS entropy (never persisted/committed), server-side observer/admin roles, audited idempotent rate-limited controls; TLS/reverse proxy deferred to the deployment audit | Any exposure beyond loopback/LAN or multi-user requirements |
| D-018 | 2026-08-04 | Proposed technical baseline | Observer shell is plain TypeScript + PixiJS v8 without React for the first slice (within ADR-0005's proposed decision); the observer requests WebGL while WebGPU preference awaits physical-device evidence | UI complexity grows past hand-rolled DOM state, or device evidence lands |
| D-019 | 2026-08-04 | Proposed technical baseline | Phase 4 persistence stack: `zstd` + bundled `rusqlite` (the ADR-0007 baseline; the two justified new dependencies), snapshots store no terrain (regenerated and checksum-verified on load), checkpoints run synchronously on the tick thread with the measured stall recorded, and the append-only event-log file plus Parquet export are deferred with a zero event-log reference in snapshots until they exist | Event-log implementation, checkpoint stall exceeding the tick budget at supported tiers, or storage-backend evidence |
| D-020 | 2026-08-04 | Accepted product direction | Long-term ambition changed: organisms should be able to evolve toward tool use, persistent structures, transmitted knowledge, technological accumulation, territoriality, and organized inter-group conflict, none of it scripted as stages. `docs/25-emergence-and-epistemic-position.md` becomes the authority on what the project claims, hopes, and predicts, and separates the three | Product direction changes |
| D-021 | 2026-08-04 | Proposed technical baseline | Governing philosophy: author physics, never progress (ADR-0012). No technology tree, research graph, era state, recipe, or civilization mechanic will ever be authored. Test for any proposed mechanism: if you can name the outcome it makes more likely, it is authored progress and is rejected | A boundary case the test does not resolve, or evidence that a specific authored capacity is needed to make a question askable |
| D-022 | 2026-08-04 | Proposed technical baseline | Analysis observes, never instructs (ADR-0016), enforced structurally: offline analysis lives in a `sim-analysis` crate that `sim-core` does not depend on, draws from no RNG stream, and its version strings are deliberately excluded from the config hash. Checksum equality with analysis on and off is a required test | Effectively never for the rule; the enforcement mechanism may change if crate separation proves unworkable and an equivalent structural guarantee replaces it |
| D-023 | 2026-08-04 | Proposed technical baseline | Phase order set to 5 headless/experiments, 6 contest, 7 genome, 8 learning, 9 social, 10 artifacts, 11 physiology, 12 detection. Contest moved ahead of the culture stack because it needs no schema change (topology 1 reserves health, threat, damage, and attack channels), it makes threat information real before a signal channel exists, and it is the highest-prior-probability positive result. Full argument in `docs/19-implementation-roadmap.md` | Phase 11 shows plasticity is selected to zero everywhere; Phase 9 cost proves prohibitive; or compute cost makes multi-seed campaigns infeasible |
| D-024 | 2026-08-04 | Proposed technical baseline | Genome schema successor (ADR-0013): diploid chromosomal genome with sorted typed loci, meiosis with crossover and linkage, evolvable dominance, and structural change by gene duplication/deletion/insertion/transposition rather than NEAT-style graph editing. Variable topology and genetic realism from one mechanism. Versioned I/O channel registry replaces hard-coded 20/12 channels. Synchronous evaluation makes activations world state. Schema 1 stays readable forever with no migration | C9.5 shows duplication-only growth too slow; evaluation cost or snapshot growth breaks a budget; a regulatory locus type becomes necessary |
| D-025 | 2026-08-04 | Proposed technical baseline | Lifetime learning model (ADR-0014): genome-encoded plasticity with evolved neuromodulation. Rule forms are a bounded authored registry; which edges are plastic, which rule, its coefficients, and what gates it are genes. **No reward function of any kind**, preserving the RL non-goal. Learned state accumulates in Q16 fixed point because float accumulation over 10^5+ ticks would undermine ADR-0011. No Lamarckian inheritance by default. Plasticity costs energy | Plasticity selected to zero across the environmental-variability sweep; sparse learned-state storage fails the checkpoint budget; cross-platform replay becomes a requirement |
| D-026 | 2026-08-04 | Proposed technical baseline | Mutable world state and ALIF format 2 (ADR-0015): the terrain regeneration invariant is split, not abandoned. Baseline still regenerates from (seed, config) and is checksum-verified exactly as in format 1; a separately checksummed modification delta is stored and applied in `(layer_id, cell_index)` order; a composed checksum is verified before the world exists. Format 1 is never reinterpreted in place and loads through a registered migration whose result must be byte-identical to a format 1 load. Objects share the organism ID space. Elevation stays immutable in this slice | Modification density makes the sparse representation useless; snapshot growth breaks the checkpoint budget; elevation mutability becomes necessary |
| D-027 | 2026-08-04 | Proposed technical baseline | Biological realism policy (ADR-0017): prefer the biological mechanism over the abstract shortcut unless the shortcut is justified in writing. Realism is bounded by determinism first, bounded state second, measured cost third. A realistic mechanism must reproduce a textbook result it was not tuned to produce, which turns the policy into acceptance criteria (Hardy-Weinberg, linkage decay, allometric exponent, lifespan versus extrinsic mortality). Genetics realism in Phase 9, physiology in Phase 13, with the non-transfer of prior results across Phase 13 stated in advance | A realism criterion fails in a way indicating the mechanism is wrong; throughput cost reduces reachable generations below what the culture phases need |
| D-028 | 2026-08-04 | Proposed technical baseline | Determinism contract extended (`specifications/determinism-extensions.md`): appended RNG stream values 7-16 (existing values never renumbered); canonical pair key `lifesim-pairkey-v1` for pairwise draws; one shared monotonic object ID space; roles assigned by ID comparison; read-prior/commit-after for all social learning; sorted-and-truncated candidate sets; float summation pinned to ascending edge `homology_id` order; lifetime accumulators in fixed point; new checksum sections appended under their own tags only when enabled, preserving fixtures `0x1e3158a26afd3b39` and `0xff9dfcff5dffbf42` | A new mechanism that cannot be expressed within these rules, which would require its own ADR rather than an exception |
| D-029 | 2026-08-04 | Superseded | The former Phase 5 (performance optimization) and Phase 7 (advanced ecosystems) plans are superseded by phases 5-12 and preserved unmodified under `planning/superseded/`. Performance work becomes a standing discipline in every phase's Benchmark Impact section; profiling, SIMD, and GPU evaluation remain backlog items requiring their own evidence | Retained for history; do not apply to new work |

| D-030 | 2026-08-04 | Accepted product direction | Three world origin modes (ADR-0021): `random` (current behavior, fixture-preserving), `seeded` (biome-matched founder archetypes, the "head start"), `scratch` (chemistry field with optional abiogenesis). Founder demes, archetypes, and founder-population files join the config. Authoring where the search begins is not authoring the path it takes, which is what makes all three admissible under ADR-0012 | Archetype inertness fails; deme seeding proves insufficient for Phase 7; a research question needs a starting condition none of the modes expresses |
| D-031 | 2026-08-04 | Accepted product direction | **ADR-0012 amended by ADR-0018**: environmental and selective structure may be deliberately shaped toward the major transitions, which fails ADR-0012's "can you name the outcome" test as written and is therefore recorded as an explicit bounded exception rather than a reinterpretation. Still forbidden: rewarding the target trait, granting it, or any recipe or stage. A scaffold must be describable without naming its target. Every scaffolded claim requires an unscaffolded control on the same seeds and is reported as the weaker claim | A transition occurs unscaffolded, making the scaffold unnecessary; a scaffold is found to grant an effective advantage; the amendment is invoked outside the transition phases |
| D-032 | 2026-08-04 | Proposed technical baseline | Modular morphology (ADR-0019): an organism is typed modules on a discrete integer lattice, grown by a developmental growth program in genome schema 3, allocating the regulatory locus type ADR-0013 reserved. Mass, speed, sensing, intake, storage, basal cost, and controller node budget all become derived consequences of the module set. Bodies are derived and **not stored**, deliberately avoiding a fourth snapshot growth term. A one-module body is a unicell, so multicellularity needs no separate mechanic | Genotype-phenotype discontinuity prevents selection acting; development cost or non-viability rate breaks a budget; the lattice proves too restrictive |
| D-033 | 2026-08-04 | Proposed technical baseline | Two-regime simulation (ADR-0020): a field/population-level microbial regime over the raster with no per-individual randomness, coupled to the unchanged individual engine. Genotype classes are a bounded discretization. Materialization at an aggregation threshold is a representation change, not an achievement, and transition neutrality is tested directly. Field state is stored because it cannot be recomputed | Conservation across the boundary cannot be made exact; field cost proves population-dependent; abiogenesis never fires usefully even scaffolded |
| D-034 | 2026-08-04 | Proposed technical baseline | Biomes, climate drift, and generator successor `lifesim-worldgen-v2` (Phase 6): moisture and temperature fields, biome classification, and a **stateless** deterministic quasi-periodic drift term evaluated from tick alone. There is no age or era state; a cold stretch is labelled post hoc by Phase 17 analysis, never entered by the simulation. v1 worlds keep v1 forever, preserving their terrain checksums and both fixtures | Environment phase cost exceeds budget; biome thresholds produce degenerate maps; elevation mutability becomes necessary |
| D-035 | 2026-08-04 | Proposed technical baseline | Phase order extended to 16 with new phases 6 (biomes/origins), 9 (morphology), 14 (abiogenesis), 15 (multicellularity); prior phases 6-12 renumbered to 7-8, 10-13, 16. Abiogenesis and multicellularity are placed **last** despite being narratively first: nothing depends on them, Phase 16 depends on Phase 10's morphology, and they are the least tractable and most likely to return null. Full argument in `docs/19-implementation-roadmap.md` | Phase 11 shows plasticity selected to zero everywhere; Phase 9 or Phase 10 cost proves prohibitive; morphology discontinuity blocks selection |
| D-036 | 2026-08-04 | Proposed technical baseline | RNG streams extended to 22 (`Morphogenesis` 17, `Abiogenesis` 18, `MicrobialField` 19, `Transition` 20, `ClimateDrift` 21, `FounderSeed` 22), appended never renumbered. `FounderSeed` is separate from `GenomeInit` so origin modes cannot shift an existing `random` world's founder sequence. Three streams are allocated but unused under default deterministic policies so a later stochastic policy cannot renumber | A mechanism that cannot be expressed within the existing ordering rules |
| D-037 | 2026-08-04 | Accepted product direction | Six commissioned research reviews reconciled against the plan (ADR-0022): fourteen findings adopted, three declined with reasons. The reviews are carried in-repo under `.agents/skills/genesis-*/references/` and indexed in `research/deep-research-index.md`; consulting them before designing is now an agent-contract requirement | A further review contradicts an adoption, or a declined finding is shown to matter by measurement |
| D-038 | 2026-08-04 | Superseded | The claim that organized violence "tends to fall out of scarcity plus kin-biased grouping" is **withdrawn** (ADR-0022 A1). The social-organization review rejects the shortcut and lists roughly eleven further dependencies. Phase 7 keeps its position but delivers only the physics of damage and contest; kin recognition, directed inter-group aggression, and coalition formation move to Phase 13 | Retained for history; do not apply |
| D-039 | 2026-08-04 | Proposed technical baseline | Phases 12 and 13 swapped so artifacts precede signalling (ADR-0022 A2, triple-sourced). An artifact left behind is a transmission channel needing no perception of conspecifics, and it is the easier one. Phase 12 delivers objects plus stigmergy as the first transmission mechanism; Phase 13 asks whether a faster channel beats that baseline | Phase 12 shows stigmergic transmission is unreachable, making the baseline vacuous |
| D-040 | 2026-08-04 | Proposed technical baseline | Perception delivers cues, not labels (ADR-0022 A3, A4): the genotype-distance kinship channel and the one-hot action-class channel are both removed, replaced by body motion, contact events, object-state change, carried-object class, and visible phenotype. Kin recognition must be solvable from perceptible cues or not solvable at all | Recognition proves unreachable from cues across the full seed set, which would itself be the finding |
| D-041 | 2026-08-04 | Proposed technical baseline | Experimental methodology tightened (ADR-0022 A5, A6, A7, A13): the world is the experimental unit and per-organism quantities aggregate to world level; 30 independent worlds minimum per confirmatory condition and 50 for rare outcomes, with pilot-driven power analysis setting the final number; one designated primary endpoint per phase with conjunctive acceptance; nulls report a smallest effect of interest, an interval, and an equivalence result; novelty claims require demonstrated consequence and persistence | Power analysis shows a different floor, or compute makes 30 worlds infeasible, in which case the number of claims drops rather than the seeds per claim |
| D-042 | 2026-08-04 | Proposed technical baseline | Structural identity split into four derived fields (ADR-0022 A8): gene lineage, homology class for alignment, structural signature, mutation event. Derived by domain-separated hash over a canonical event key, removing the world-global monotonic counter and making independent identical mutations converge on the same homology class | A hash collision policy proves insufficient, or a field turns out to be redundant in practice |
| D-043 | 2026-08-04 | Proposed technical baseline | Controller evaluation becomes hybrid (ADR-0022 A9): canonical topological order over zero-delay edges plus prior-state buffers for delayed and recurrent edges, replacing the all-synchronous update that cost one edge of propagation per tick. A cycle among zero-delay edges is a decode-time error. Also adopted: inheritance-mode controls including clonal and single-parent whole-genome (A10), bounded event memory (A11), intermediate culture rungs (A12), and removal mutations with explicit evaluation cost (A14) | Topological ordering proves too costly per tick, or the event-memory cap breaks the snapshot budget |
| D-044 | 2026-08-04 | Deferred | Developmental encoding stays the morphology baseline against two reviews' advice (ADR-0022 D1), because the one-module-is-a-unicell unification removes the need for an authored multicellularity mechanic. Concessions: the controller stays directly encoded, the program is a bounded versioned module, the direct parameterized body plan is a specified fallback, and Phase 10's discontinuity measurement is promoted to a pass/fail gate. 2.5D height and support graph deferred to a Phase 12 stretch item; until it lands the plan does not claim stacked construction (D2) | Phase 10's discontinuity gate fails, taking the fallback; or Phase 12 cost leaves room for the height subset |

| D-045 | 2026-08-04 | Proposed technical baseline | Phase 5 instrument implemented. **Event log**: ALEV format 1, append-only per-tick segments with a self-checking header (provenance is not framing, so nothing downstream would catch a corrupted seed or config hash), CRC-per-segment, every declared length capped before allocation, unknown event types fail closed, and a separate prefix reader that reports a torn tail without repairing it. Snapshots now carry a real `event_log_offset`, closing D-019. **Experiment harness**: new `sim-experiment` crate carrying conditions (named config deltas with their own hashes), campaigns, the independent-world scheduler, manifests with embedded verbatim campaign source, and a comparison report that refuses rather than aggregates. **Async checkpointing**: capture on the tick thread, write on another, capacity-one queue that refuses and counts rather than queueing without bound. **Server pacing**: `--pacing headless` and `--run-ticks`, making acceptance criterion A5.1 an automated test rather than a manual procedure | Scheduler equality fails on another host or core count; event-log growth breaks a disk budget; the capacity-one checkpoint queue proves too strict for a slow filesystem |
| D-046 | 2026-08-04 | Proposed technical baseline | Two Phase 5 additions the plan did not ask for, both adopted because their absence would have made a declared experimental design silently wrong. **Campaign preflight**: every declared world is constructed at tick 0 before any ticks run, and a campaign that cannot build all of them is refused. World generation rejects seeds whose land fraction falls outside bounds, so a declared 30-seed design would otherwise execute as a 27-seed one, with the missing seeds selected by a terrain property that may correlate with the outcome. **Load-time condition validation**: two conditions producing the same effective config hash, or differing in a field no `vary` directive declares, are both rejected before the campaign runs. Cost: one world generation per unit, negligible against any serious run length; `--no-preflight` exists for deliberate use | A legitimate design needs a seed set with known-unbuildable members, which would need its holes recorded in the manifest instead |

| D-047 | 2026-08-04 | Proposed technical baseline | Phase 6 climate slice implemented (origin modes not started). Generator selection becomes a **config field** rather than a constant, so the selected version enters the config hash and a v1 world's hash is provably unmovable by adding v2; both fixtures verified byte-identical. Temperature is **derived, not stored**, departing from the specification sentence that lists it under `lifesim-climate-state-v1`: under the default deterministic policy it is a pure function of `(base, tick)` and carries no history, which is exactly the "derived, recomputed on load" category the same document defines. Only moisture, a genuine integrator, is stored. If a later policy adopts the stochastic weather term stream 21 reserves, temperature becomes an integrator and joins the section | A stateful weather policy; or a climate parameter needing per-cell history |
| D-048 | 2026-08-04 | Proposed technical baseline | Two Phase 6 modelling defects found by the acceptance criteria and fixed, recorded because the general lesson will recur: **a conservation law is not a correctness proof.** (1) Moisture derived from elevation alone makes the biome map a relabelling of the elevation map, so `Highland` and `Arid` select the same cells and one is always empty; fixed by adding breadth-first sea proximity as an independent second driver. (2) A conserving *diffusion* update satisfies exact conservation and still erases the world, because diffusion's only fixed point is a uniform field - worlds generating with seven biomes had lost `Wetland` by tick 5,000. Replaced with a pairwise exchange relative to a static per-cell holding capacity, whose fixed point is a maintained gradient; conservation remains exact and a regression test asserts the spread survives 20,000 ticks | A moisture model needing genuine sources and sinks, which would replace exact conservation with a ledgered budget |

| D-049 | 2026-08-04 | Proposed technical baseline | Phase 6 origin modes implemented. **Archetype IDs are absent from world state, not merely inert**: the specification says the kernel stores one for provenance and nothing reads it, but storing an authored label on every organism and trusting future code not to read it is a standing invitation. An archetype influences the genome it draws and leaves no trace; C6.5 is true by construction rather than by discipline. Which archetypes seeded a run lives in the config hash and the run manifest, which is where a report legitimately needs it. Founder draws key on an archetype's array **position**, not its ID, and archetypes are validated as sorted by ascending ID so position order and the specified `(archetype_id, draw_index)` allocation order coincide. Demes get per-deme trait centres rather than merely per-deme draws, because two groups sampled from one distribution have within-group distance equal to between-group distance and C6.3 would fail for a missing mechanism rather than a bug | A research question needs per-organism origin provenance, which would need its own ADR because it reintroduces an authored label |
| D-050 | 2026-08-04 | Proposed technical baseline | Phase 6 measured cost (`phase6-local-20260804T224418Z`) answers the plan's open question: the **reclassification cadence does not need a dirty-cell strategy, the moisture exchange does**. Reclassifying every 100 ticks is free (473 vs 481 microseconds, inside the spread); reclassifying every tick more than doubles the environment phase, confirming the cadence is load-bearing. The per-tick moisture exchange adds ~430 microseconds to a 51-microsecond phase and cuts whole-tick throughput 4x, from 7,296 to 1,793 ticks/s at 65,536 cells. A moisture cadence or dirty-region scheme is now a measured backlog item | Profiler evidence that the exchange is not the dominant term, or a moisture model with different cost structure |

| D-051 | 2026-08-04 | Proposed technical baseline | Phase 7 contest physics implemented: health, damage, healing, death by depletion, the attack action on reserved output 4, health/threat/recent-damage on reserved inputs 2/10/16, carcasses with their own exact ledger, canonical pair key `lifesim-pairkey-v1`, RNG stream `Contest` (7), and event schema 3. No genome schema change; topology 1 already reserved every channel. Threat is a **perceptible phenotype cue** (relative body size and closeness), never genotype distance, per ADR-0022 A3. Damage deaths are counted in the contest section rather than in `Counters` so the Phase 1/2 checksum field list is untouched, and carcasses are inserted in ID-sorted position rather than appended because an older organism can die later than a younger one | A contest mechanism that cannot be expressed within the reserved channels, which would need the genome successor |
| D-052 | 2026-08-04 | Proposed technical baseline | Phase 7's behavioral campaign ran and **did not meet C7.2's tolerance clause or C7.3**, and the criteria are recorded as unmet rather than adjusted. A fourth condition D (contest enabled, attack threshold above the controller's output range) was added beyond the three the plan specifies, because conditions A and C each change two things at once: they make the attack action live *and* the reserved sensing channels live. D isolates perception. Result across 30 seeds: perception alone costs nothing (median population 48, identical to baseline), the attack action alone costs ~37 percent at zero damage via its energy cost at a saturating attack rate, and damage costs a further ~31 percent. **C7.1, the designated primary endpoint, was not measured**: no world-level spatial-aggregation index exists, so Phase 7 is not complete | The spatial index lands and the campaign is re-run; or the saturating attack rate is judged a cost-structure defect, which would open `contest-behavior-v2` |

| D-053 | 2026-08-04 | Accepted product direction | Two run modes (ADR-0023): **campaign worlds** (30-50 short independent worlds, maximum speed, disposable, the basis for every claim) and **flagship worlds** (one world, open-ended horizon, 1x default, preserved and backed up, watched). Neither changes kernel semantics. **A flagship world may never support a claim: n=1.** A flagship observation becomes a campaign proposal, with the flagship excluded from that campaign's seed set so the observation that motivated a design is not also its evidence. Compute is an explicit allocation, not an emergent split | Soak criteria fail, making unattended operation unsupported; a flagship observation appears in a report as a claim; compute pressure makes the reservation untenable |
| D-054 | 2026-08-04 | Proposed technical baseline | Time-scale position recorded (`docs/27-time-scale-and-pacing.md`), which had never been written down: measured base rate is roughly 1,575 ticks per ancestry generation, so 1x gives about 23 generations/hour and 16,500 per 30 days. 1x is the flagship default. `dt` stays 100 ms and versioned. Speed multiplication never changes results, guaranteed by Phase 5's acceleration-neutrality criterion. Pacing is presentation and is never fixed by changing behavioral policy | Later phases move ticks-per-second materially, which each phase's Benchmark Impact section records; Phase 14 evolvable life history destabilizes the generational clock |
| D-055 | 2026-08-04 | Proposed technical baseline | Long-horizon soak tiers (`specifications/long-horizon-soak.md`): Soak-7, Soak-30, Soak-90 against the current 864,000-tick verified maximum, which is 30 times short of a 30-day flagship run. Eight conjunctive criteria covering storage growth, tick-time drift, memory stationarity, invariants throughout, checkpoint restore at the horizon, clean-process determinism at the horizon, structural-quantity stationarity, and an explicit record of whether the world was still doing anything. **Soak-30 gates the flagship mode** and is currently unmet. Re-run after any phase that adds a growth term | A tier fails, deferring flagship availability; a growth term is added by a later phase |
| D-056 | 2026-08-04 | Proposed technical baseline | 3D voxel rendering with derived appearance (ADR-0024, amending ADR-0005). Governing rule: **author the visual primitives, not the appearances.** A palette of roughly fifteen primitives (one per module type, material, and biome tint); every organism, artifact, and structure is an arrangement of those taken from simulation state, so no asset library caps what the simulation may contain. **No generative models in the render path**: an image model asked what a creature looks like answers from its prior rather than from the organism, so the picture stops being evidence; also zero cache hit rate against unique genomes and broken reproducibility. Offline palette authoring and event-log narration remain permitted. Rendering dimensionality is not simulation dimensionality: the simulation stays 2D and the 3D view is a presentation with no kernel, determinism, or fixture impact | Voxel rendering cannot hold frame rate at the supported tier; the ADR-0022 D2 height subset lands, enabling stacked construction; sphere-world geometry is adopted |
| D-057 | 2026-08-04 | Proposed technical baseline | Former Phase 13 split by ADR-0025, amending ADR-0017's placement argument. **Phase 8** (allometry, thermoregulation, senescence, non-food extrinsic mortality, juvenile mortality, life-history tradeoff, death-cause accounting) executes **after Phase 7**; it has no unmet prerequisites and delivers the non-food mortality that produces per-capita surplus. **Phase 14** (developmental ontogeny, mate choice on perceived phenotype, optional disease) stays after Phase 13 because ontogeny needs the Phase 10 module body and mate choice needs Phase 13 perception. Motivation: the Phase 2 record shows 199,871 starvation deaths against 180 old-age deaths with population pinned on the `max_entities` guard, so every energetically gated culture criterion would have returned a null caused by starvation rather than by anything about transmission. Accepted cost: per-organism cost arrives earlier and applies to every subsequent phase; Phase 7 results do not transfer across Phase 8 | C8.1 shows the death-cause distribution does not become mixed; the measured throughput cost outweighs running invalid campaigns; ecology cannot bind below an affordable `max_entities` |
| D-058 | 2026-08-04 | Proposed technical baseline | Intra-world parallelism authorized under conditions (ADR-0026), amending `specifications/determinism-extensions.md` Rule 10 which forbade it outright. Shape: freeze read state, partition by `f(object_id, P)` with `P` a config constant **independent of thread count**, workers emit intents only, merge canonically in `(partition_index, object_id)` order, resolve conflicts under existing policies, commit once single-threaded, reduce over a fixed tree. **Tier 1 (thread-count invariance) is proposed** rather than the Tier 2 target originally set, because the methodology review recommends partitioning by stable ID rather than worker count and because this project's all-fixed-point state makes cross-partition reductions order-independent by construction (the ledger is `i128`; no cross-organism float accumulation exists). Tier 2 (thread count in the config hash) is the defined fallback. Tier 3 (true non-determinism) is **not available**: it would remove fail-closed checkpoint verification on the one world whose checkpoint chain is irreplaceable. Campaign worlds stay single-threaded and remain the basis for every claim | Tier 1 fails and the fallback's lineage cost is taken; a future phase adds a cross-organism float reduction; the measured serial fraction makes the speedup not worth the complexity; population turns out not to be the binding constraint |
| D-059 | 2026-08-04 | Proposed technical baseline | **Phase numbering made monotonic in one atomic pass**, discharging the renumber ADR-0025 left outstanding. Final sequence: 5 headless/experiments, 6 biomes/climate/origins, 7 contest, 8 demography, 9 genome, 10 morphology, 11 learning, 12 artifacts, 13 social, 14 ontogeny/sexual selection, 15 abiogenesis, 16 multicellularity, 17 era detection, 18 parallelism. Mapping applied: `13a` to 8, 8 to 9, 9 to 10, 10 to 11, 11 to 12, 12 to 13, `13b` to 14, 14 to 15, 15 to 16, 16 to 17, 17 to 18. Eleven phase files renamed; 75 files updated by text substitution; nine phase-column tables reassigned **by content** rather than by the mapping, two of which had been stale since the original numbering and were missed by both prior renumbers. The execution order is unchanged  -  this is relabelling only, and no acceptance criterion, ablation, or dependency moved. Numbering in D-023 and D-035 predates this pass and is left as the historical record | A phase is inserted, removed, or reordered again, which requires a new pass and a new entry rather than an in-place edit |

| D-060 | 2026-08-05 | Proposed technical baseline | **Phase 7 completed: C7.1 measured and met.** The world-level index it needed is `lifesim-spatial-index-v1` in a new `crates/sim-analysis`, computed offline from a new versioned spatial-sample artifact (`ALSS`, format 1) that the experiment harness writes through the existing read-only observer view -- **no kernel change, so both fixtures are unmovable by this work**. Positions are deliberately *not* added to the event log: ALEV records what happened, and a per-tick coordinate dump would multiply its size by three orders of magnitude while making "one event, one thing that happened" false. The index is two nested Morisita quadrat scales over habitable area (8 m and 64 m), because the conditions differ several-fold in population and Morisita's expectation is 1 under complete spatial randomness at any population size; the encounter measure is the fine-scale index divided by the coarse one, which is exactly 1 when fine-scale spacing is random given coarse-scale position. All integer arithmetic; the bootstrap draws on a new `Analysis` RNG stream (41) that no tick ever touches. Result over 60 seeds x 5 conditions: **52/60 and 44/60 against a prespecified bar of 40, both indices, conjunctive -- C7.1 passes**, with zero worlds excluded | The index is superseded by a better spatial statistic; or a phase adds a mechanism whose spatial signature the two chosen scales cannot see |
| D-061 | 2026-08-05 | Proposed technical baseline | **Two corrections the C7.1 campaign forced, both recorded rather than smoothed over.** (1) **The criterion's literal decision rule does not discriminate.** "Differs by at least the SESOI in at least 20 of 30 worlds" is passed by a condition with no effect: pilot condition D, mean effect +1.4 percent, crossed a 10 percent SESOI in 12 of 16 worlds, because per-world seed variation exceeds the SESOI. The rule is therefore **directed** -- worlds must cross in a direction fixed from the pilot, before the confirmatory run, on disjoint seeds -- which is strictly stronger than the criterion as written and makes the 20-of-30 bar an exact one-sided sign test at a null rate of 0.5. Under it, D scores 22/60 and 23/60 and correctly fails. (2) **A claim made from pilot data was refuted by the confirmatory data and is withdrawn, not softened.** The pilot's 16 worlds suggested neither index tracked population; 60 worlds show rho +0.41 for aggregation and -0.56 for encounter within the baseline condition | A future phase's criterion is written with a count-based bar again, which should reuse the directed form from the start |
| D-062 | 2026-08-05 | Proposed technical baseline | **What Phase 7 actually established, which is narrower than "contest changes spatial structure".** The aggregation half of C7.1 is **confounded**: population predicts its direction, and condition E -- contest disabled, carrying capacity cut -- reproduces most of it (-12.8 percent, 36/60) with no contest in it at all. The encounter half runs **against** its confound: lower population predicts a *higher* encounter index (rho -0.56 within B, and E measures +51.1 percent in 47 of 60 worlds), yet full contest measures -24.5 percent. The carried claim is therefore: **damage reduces short-range co-occurrence conditional on position by about a quarter, and the accompanying population decline pushes that measure the other way.** Damage is the cause, not the action: condition C fires attacks at thirteen times A's rate with damage exactly zero and moves neither index. **C7.3 stays unmet**, now on its contention-correlation clause (rho +0.19) rather than its saturation clause -- at the standard tier the attack rate is 0.51 percent of the cooldown ceiling, so **no `contest-behavior-v2` is opened**; the saturation that motivated the question appears only under the zero-damage condition where nothing damps the action | Phase 8's non-food mortality changes the population trajectory enough to alter the confound structure, which would require re-measuring both halves |

| D-063 | 2026-08-05 | Proposed technical baseline | **Phase 8 implemented; C8.1 met, three secondary criteria unmet and recorded as unmet.** New config-gated physiology section (`lifesim-demography-v1`) with six independently gated mechanisms, RNG stream `Mortality` (16), checksum section `lifesim-physiology-state-v1`, and death causes `Senescence` and `Extrinsic` appended to the event schema. Three choices carry determinism: **allometry uses exact integer roots rather than `powf`** (a quarter-power is two integer square roots, covering 0.25 to 1.5 including Kleiber's 0.75); **senescence is Weibull rather than Gompertz**, because a Gompertz hazard needs `exp`; and **senescence and extrinsic mortality draw separately on distinct draw indices**, because they are competing risks and C8.1 is a claim about which of them killed what - a single combined draw would make the death-cause distribution unattributable. The phase's purpose is achieved decisively: starvation share falls from **1000/1000 under the baseline to 494/1000**, 26 of 30 worlds meet the primary endpoint against 0 of 30 for the baseline, the food field sits at 99 percent of capacity rather than 74, and all 120 worlds are below 90 percent of `max_entities`, removing the artifact the phase existed to remove | A later phase changes the energy budget enough to move the mortality mix, which would need the sweep re-run |
| D-064 | 2026-08-05 | Proposed technical baseline | **Phase 8's three unmet criteria, recorded rather than adjusted.** **C8.5** (evolved lifespan responds to extrinsic mortality) is a **measured null**: 16 of 30 worlds against a bar of 20, median completed lifespan 1,084 against 1,052 ticks. The direction is right and maximum observed age orders as predicted across all three mortality regimes (15,677 / 14,170 / 12,192), so the tail moves while the median does not; and M-high sits near viability, six of its thirty worlds below ten organisms, so its estimate rests on far fewer completed lifespans. **C8.6** (life-history tradeoff) is 13 of 30 under the treatment. **C8.7** (thermal preference) fails on its *control*: the criterion requires the correlation absent under the inert-gene condition and it is not - median rho 38, positive in 21 of 30 worlds - because biome capacity correlates with temperature, so organisms follow food into thermally non-random cells whether or not they can feel temperature. Paired against its own control the treatment exceeds it by a median +134 milli in 19 of 30 worlds, one short. The reading carried forward is that thermoregulation probably does something and this design cannot demonstrate it | A control that separates food distribution from temperature, which needs either a decoupled capacity model or a within-world randomization of the gene |
| D-065 | 2026-08-05 | Proposed technical baseline | **Two pre-existing persistence defects found while wiring Phase 8, both fixed.** (1) **ALIF snapshots never encoded any config section added after Phase 2.** Climate, origin, contest, and physiology were silently dropped on save and restored at their defaults. For climate and contest the presence check turned that into a confusing restore failure; for the origin section, which has no presence check, a `seeded` world would have restored as `random` **with no error at all**. Format bumped to 2 with no migration from 1, because a format-1 file cannot say what its climate settings were and inventing them would violate the "never alter meaning during load" rule. The structural defence is a test that perturbs every field, round-trips, and compares the whole `SimConfig` with one assertion, so a field added later and left unencoded fails immediately. (2) **The biome map was documented as derived and recomputed on load, and is not.** Reclassification runs on a cadence, so the map a world holds reflects the last reclassification tick; recomputing it at the restore tick produced a different map and the restored world diverged on the next tick through biome-dependent carrying capacity. It is now stored state. D-047's "derived, recomputed on load" category covers pure functions of current state; a classification cached on a cadence is not one | A future subsystem caches a derived field on a cadence, which is the same trap and needs the same treatment |

| D-066 | 2026-08-05 | Proposed technical baseline | **A three-way contradiction about controller evaluation, found on starting Phase 9 and resolved in favour of the recorded decision.** `specifications/neural-network-schema.md` and ADR-0022 A9 (D-043) specify **hybrid** evaluation - zero-delay edges in canonical topological order over the acyclic subgraph, delayed and recurrent edges from prior-state buffers - and say explicitly that it *replaces* the all-synchronous update. ADR-0013 and `planning/phase-9-evolvable-genome.md` still said synchronous, in three places, because neither was updated when ADR-0022 landed. Hybrid wins: it is the later decision, it is in the decision log, and it is in the normative specification. The stale text is struck rather than deleted, so the supersession is visible. **Consequence for the encoding**: hybrid requires every edge typed zero-delay or delayed, and `genome-schema-2.md`'s Edge flags byte had no delay bit even though its own `structural_signature` row already listed delay as phenotype-relevant. Bit 2 is now the delay bit and bits 3-7 are reserved and must be zero. A zero-delay cycle is a decode-time error, because under the hybrid update it has no fixed point and the honest response is to refuse the genome rather than invent an iteration order that produces a number | A later phase needs zero-delay cycles, which would require an iteration policy and its own ADR |
| D-067 | 2026-08-05 | Proposed technical baseline | **Phase 9 first slice: registries, the schema 2 genome, the ALG2 codec, and expression.** Channel and activation registries replace schema 1's hard-coded 20 inputs and 12 outputs, populated with exactly the post-Phase-7 channel set - 16 sensory and 8 action - and **deliberately without topology 1's four memory registers**, which under schema 2 are recurrent edges the organism evolves rather than a register file the kernel wires. Identity is ADR-0022 A8's four fields, with `homology_id`, `gene_lineage_id`, and `mutation_event_id` stored and `structural_signature` **computed on demand**, because a stored derivation can drift from what it derives. IDs are derived by domain-separated hash rather than allocated from a counter, which buys the property a counter cannot: two lineages that independently evolve the same structural change converge on the same `homology_id` and therefore align during meiosis. Structural caps are **provisional** and C9.8 requires them restated against a measured snapshot budget. **C9.6's codec half is met**: 100,000 seeded malformed cases, zero panics, zero invalid admissions, every accept re-validated and round-tripped. **C9.7's fixture obligation holds**: a schema-1 world still reproduces `0xff9dfcff5dffbf42`. Meiosis, structural mutation, controller v2, and world integration are **not** implemented | The caps measurement moves them; a later phase needs a locus type, which is a registry-style append rather than a schema bump |

| D-068 | 2026-08-05 | Proposed technical baseline | **The specification's crossover model was not Mendelian, in two separable ways, and both were found by C9.3 and C9.4 failing rather than by inspection.** (1) *The walk always began on haplotype 0*, so the first locus of every chromosome was inherited from slot 0 unless a crossover landed exactly on it; measured transmission of the second allele at a heterozygous marker was **0.14 against an expected 0.5**. Fixed by drawing the starting side, which is what meiosis does - which chromatid a gamete begins reading is not fixed. (2) *Recombination fractions exceeded one half*, reaching **0.64** across a chromosome, which no crossover model may produce: `r <= 0.5` is a hard constraint of Mendelian genetics. The cause is that the specification flips at every crossover, while a real crossover occurs in a four-strand bundle and involves only two of the four chromatids, making a gamete recombinant with probability one half rather than certainty. Fixed by flipping with probability one half at each crossover. With both corrections the measured map function is textbook - 0.016, 0.031, 0.063, 0.117, 0.206, 0.360, 0.493 at distances 1 to 63 - monotone, asymptotic to 0.5, never above it. **Neither defect would have announced itself**: they would have biased every allele frequency and every linkage measurement in every later phase, silently | A crossover interference model, which would change the position distribution but not these two corrections |
| D-069 | 2026-08-05 | Proposed technical baseline | **Meiosis and structural mutation implemented; two specification tensions resolved.** (1) *Duplication could not satisfy three of the specification's requirements at once*: strictly ascending `homology_id` as a decode invariant, duplicates inserted immediately after their source because adjacency in innovation order **is** linkage, and IDs derived by hash so independent identical mutations converge. A hash over the whole ID space forces a re-sort, which scatters the duplicate away from its source. Resolved by hashing into a **small offset from the source ID** (`DUPLICATE_SPAN` = 16): sortedness is exact, the duplicate stays linked, and convergence survives because the offset is still a pure function of the mutation description. An occupied slot is rejected and counted, never relocated. (2) *Duplication selected its run uniformly over the chromosome*, which for a minimal founder landed on a trait locus about two thirds of the time and silently made the realized rate a third of the configured one - so a slow structural-evolution result would have been an artifact of trait storage. The run now starts among structural loci only. Every rejection is typed and counted in world state, because an experiment quietly running against a cap has to be visible in its own report | The caps measurement (C9.8) changes `DUPLICATE_SPAN`, or a trait-duplication operator is wanted, which would need a second slot per `trait_id` |

| D-070 | 2026-08-05 | Proposed technical baseline | **Controller v2 implemented as the hybrid D-066 settled** (`lifesim-controller-v2`). Zero-delay edges evaluate in a **canonical topological order** over the acyclic subgraph - Kahn's algorithm with the ready set drained in ascending `homology_id`, so the order is a pure function of the graph and never of storage layout - while delayed and recurrent edges read prior-state buffers, which breaks every cycle by construction and needs no relaxation, no convergence assumption, and no cycle special case. Measured: a signal crosses a two-edge chain within one tick under zero-delay edges and takes one tick per edge under delayed ones, which is exactly the capability the fully synchronous update could not have had. **Per-node incoming edges are summed in ascending edge `homology_id` order with zero-delay and delayed edges interleaved rather than grouped by kind**, so that ordering is the only thing deciding the sum; the test that pins it uses weights straddling f32 epsilon, because an earlier draft used 1e8 and 1.0, which round identically in both orders and would have passed while demonstrating nothing. Activations and prior-state buffers are **world state**, saved and checksummed under `lifesim-activation-state-v1`, because a recurrent organism's memory lives there and recomputing it on load would silently reset it. Schema 1's fixed four-value memory register file has no counterpart: under schema 2 the same thing is a recurrent edge the organism evolves, which is why the channel registry deliberately omits the memory channels | A phase needs zero-delay cycles, which would need an iteration policy and its own ADR |
| D-071 | 2026-08-05 | Proposed technical baseline | **The zero-per-tick-allocation property is preserved rather than lost.** The Phase 9 plan requires it to be preserved "or its loss explicitly recorded". A first cut allocated a gather buffer and a request vector on every evaluation; both now live as reusable buffers - the gather buffer inside `ActivationState` as **scratch, excluded from the checksum** because it is rebuilt before it is read each tick and hashing it would make the checksum depend on where in the tick it was taken. The property is asserted by a test that watches every buffer's capacity across 500 ticks rather than by a comment, because a `Vec` that reallocates is a `Vec` whose capacity changed | The evolved topology distribution makes per-organism buffers dominate memory, which is a C9.8 measurement question |

| D-072 | 2026-08-05 | Proposed technical baseline | **Schema 2 wired into the world through a deliberately narrow seam.** Enabling `genome2` replaces exactly two things - the genome and the controller - and nothing else. Movement, feeding, pairing, contest, and physiology run the same code, and the action channels map onto the same twelve output slots topology 1 produced, so nothing downstream knows which schema produced an intent. That is not tidiness: if schema 2 also rewrote the tick, C9.1's "structure evolved" and C9.2's "the ecology is still stable" would be comparisons between two different simulations and neither could be attributed to structural freedom. Founders re-express the traits the **existing** origin process drew, so deme centres, archetypes, and biome-matched placement are unchanged and the two schemas differ in structure rather than in trait distribution. A schema-2 world keeps the flat genome array empty rather than carrying a placeholder; the genome **hash** is kept for both schemas, because "which genome does this organism have" is a question every schema can answer and the observer asks it. New checksum section `lifesim-genome2-state-v1`, snapshot section 10, and a `Schema2Desync` invariant, because every parallel-array subsystem here has needed one | A phase needs a mixed-schema world, which the specification forbids and which would need its own ADR |
| D-073 | 2026-08-05 | Proposed technical baseline | **Structural evolution is birth-limited, and a C9.1 campaign must be configured for that or it will measure the birth rate instead.** The world-integration test's first configuration - the standard small test world at the default carrying capacity - produced 25 births in 6,000 ticks. Eleven structural mutations were applied and the mean node count did not move at all, leaving one distinct structure in the population: the mechanism was working the whole time and the world simply had too few generations to show it. Raising carrying capacity gives 191 births, mean nodes 3.0 to 3.6, and 14 distinct structures on the same seed and the same mutation rates. **This is the Phase 8 failure mode recurring one phase later**: a null caused by the ecology rather than by the mechanism under test. C9.1's campaign therefore needs its birth rate stated and checked before the run, exactly as Phase 8's mortality regime was swept before its campaign | A cheaper way to raise generation count than raising carrying capacity, which changes the ecology the structure evolves in |

| D-074 | 2026-08-05 | Proposed technical baseline | **Three defects found while calibrating the Phase 9 campaign, all in the path the campaign would have exercised, and one of them a crash.** (1) **Validation checked each haplotype separately while `compile` saw the merged network.** Expression takes the union over homology IDs, so haplotype 0 carrying `A -> B` and haplotype 1 carrying `B -> A` is two acyclic haplotypes whose expression is a zero-delay cycle - and meiosis assembles exactly that whenever a crossover splits a reciprocal pair. The genome validated, `compile` refused it, and the birth path then **pushed the core arrays before asking whether the organism could be admitted** and `continue`d on refusal, under a comment asserting the arrays stayed in lockstep. They did not; the next sense phase indexed `phenotypes` out of bounds and panicked. Both halves fixed: validation now re-runs the same Kahn pass over the expressed network (reusing `express_network` rather than a second merge rule written to match, because two merge rules that must agree forever is how this returns), and admission happens before anything is pushed. (2) **Meiotic recombinants were never validated at all.** Crossover can leave an edge whose node stayed on the other side of the cut, so a zygote could carry a dangling reference. Refused now at pairing as `PairRejectReason::Nonviable` alongside the other pairing rejections and before either parent pays, so an inviable recombinant costs a mating opportunity rather than the ledger. This is a real genetic outcome, not an error, and it is counted. (3) **`rejected_invalid` was documented as "a bug report, not a runtime condition" and was structurally incapable of meaning that.** Transposition on a single-chromosome genome - which every founder is, so this is permanent - was counted as `Invalid`, and the operators were being handed unvalidated recombinants, so each reported *its input* as invalid. Split into `Inapplicable` (precondition does not hold) and `Cycle` (expected for insertion), and the recombinant is validated before the operators run. `rejected_invalid` is now 0 across every regime measured | An operator gains a precondition that needs its own reason, which is an append rather than a re-categorization |
| D-075 | 2026-08-05 | Proposed technical baseline | **The Phase 2 snapshot section's length check was an exact equality over its own field count, and adding a counter broke every snapshot in the build.** The check required `count * record_len + 8 + 7 * 8` to equal the body length exactly, where `7` was the number of `Phase2Counters` fields. This is the same defect the climate section had in Phase 8 (D-065) and it broke the same way for the same reason. Replaced with a **cap** - `count * record_len <= body.len()` - which is what the fail-closed rule actually requires before allocation; exactness is still enforced by the trailing-bytes check every section already runs at the end, and that check needs no editing when a field is added. **The general rule: a decode-time length check may bound an allocation, but must never encode a field count.** Every remaining exact-equality length check is a latent instance | A section whose trailing-bytes check cannot run, which would need the exactness restored some other way |

| D-076 | 2026-08-05 | Proposed technical baseline | **A schema-2 world could not be checkpointed, and the test that should have caught it tested the wrong path.** The Phase 2 snapshot section drove its per-organism loop from `traits.len()`, which is the organism count in a schema-1 world and **zero** in a schema-2 world, because schema 2 carries no flat genome. Every schema-2 snapshot therefore encoded zero per-organism records and dropped heading, speed, last turn, parents, depth, child count, birth tick, and memory. It failed closed on restore rather than corrupting, which is the only reason it was not worse. `phase9_world::a_schema_2_world_is_deterministic_and_restores_identically` passed throughout, because it exercises `export_state` to `from_state` and never touches the codec - **the logical path and the encoded path are different paths, and a round-trip test that skips the codec is not a round-trip test.** Format bumped to 3: the section now writes the organism count and the flat-genome count separately, so the two are never again assumed equal, and a count that is neither zero nor the organism count is refused. Found by C9.8's snapshot measurement, which is the first thing that ever asked a schema-2 world for a checkpoint | A third per-organism array whose length differs again, which would need its own count rather than another assumption |
| D-077 | 2026-08-05 | Proposed technical baseline | **The schema-2 checksum silently diverged across save and restore, because a counter list was maintained by hand.** The snapshot's schema-2 section wrote its `MutationCounters` as a literal list of eleven `counters.x` reads; two counters were added (D-074) without it, so they were dropped on save - and since the counters are hashed into the state checksum, a restored world's checksum differed from the one it was saved from. Now written through an **exhaustive destructuring with no `..`**, which fails to compile when a field is added, exactly as the struct literal in the manifest parser does. The general rule this makes explicit: **serialization code must destructure, never field-access, or the compiler cannot help.** Every remaining hand-maintained field list in a codec is a latent instance of this and of D-075 | A field the format deliberately omits, which then needs its omission stated in the pattern rather than by absence |

| D-078 | 2026-08-05 | Proposed technical baseline | **The structural caps were mutually inconsistent, and three of the four could never bind. Restated from measurement (C9.8).** Measured marginal cost of a structural locus is 44.4 bytes against a 1,229-byte fixed cost for a founder's header and trait block, so `max_genome_bytes` of 16,384 admits about **341 structural loci**. Against that, `max_nodes` (256, checked across both haplotypes, so 512 node loci) and `max_edges` (1,024, so 2,048) would have needed **58 KB**, and `max_loci_per_chromosome` of 512 exceeded the byte budget on its own. The byte cap always bound first and the other three were numbers that read like guards. The rule adopted: **every cap must be individually reachable within `max_genome_bytes`**, which stays the joint budget. New values: `max_nodes` 160, `max_edges` 160, `max_loci_per_chromosome` 160, `max_edges_per_node` 32; `max_genome_bytes` unchanged at 16,384, measured affordable at 32.8 MB worst case against 3.35 MB actual at the 2,000 tier. The restated set turns out to be **jointly** reachable, not merely individually: node plus edge caps need 15,426 bytes against the 16,384 budget. Measured `rejected_cap` is **0** over 30,000 ticks at the campaign's most aggressive duplication rate, with the largest genome at 1,668 bytes and 6 nodes, so the caps are guards rather than a selection pressure at campaign scale - still 23x and 40x the largest evolved values. **Explicitly not validated for flagship scale**: Soak-30 is ~16,500 generations against the 61 measured, and duplication above the deletion rate is a growth process with that much longer to act, so genome size becomes a quantity Soak-30's stationarity criterion (D-055) must watch. Secondary measurement, worth recording because it inverts the plan's stated risk: **schema-2 snapshots are smaller than schema-1's** - 1.68 to 1.88 KB per organism against Phase 4's ~2.8 KB - because a minimal evolved topology costs less than a fixed 20-16-12-12 one, so diploidy's doubling is more than repaid; and the tick is **1.4x to 1.6x faster** under schema 2 for the same reason, which is a statement about how little structure evolved rather than about the evaluator | A regime or a horizon that pushes the evolved distribution toward the caps, which would make them bind and require the measurement re-run |
| D-079 | 2026-08-05 | Proposed technical baseline | **C9.1, C9.2 and C9.5 measured across 210 worlds; all met, and the rate at which C9.1 is met is the actual result.** The criterion's "stated amount" had never been stated, so it was fixed before the run at founder + 1 median node or edge, with the duplication rate swept at 1x, 3x and 10x its documented default and **every rate reported**, so there is no primary arm to select within afterwards. **At the shipped default rates the median does not move in a single world of thirty.** That is not a null about the mechanism: duplication and deletion ship at the *same* rate, so genome size sits at mutation-deletion equilibrium with no expected growth, and the standing variation is real - nine distinct structures per world, mean node count 3.094 against a founder's 3.000 - it simply never fixes. The median is a fixation-scale statistic. Ten times the duplication rate gives 29 of 30; **insertion enabled at the default rate gives 30 of 30**, so insertion buys at 1x what duplication needs 10x for, and the two grow different things (duplication nodes, insertion edges). The diversity clause is 30/30 under every treatment and 0/30 under both controls, which is the phase's cleanest separation. C9.2 passes at every rate on both population and lifespan, and also by TOST equivalence, which is stronger than the count bar; zero extinctions in 210 worlds; the cost of structural freedom is monotone and small (population -4.7 percent at A10, -5.2 percent at A10i). **ADR-0013's duplication-only baseline is retained as the default and insertion is not promoted**, because duplication does work at a higher rate and is the biologically motivated mechanism; what changes is that the shipped rates are now known to sit below the threshold at which duplication produces population-level structural change in 60,000 ticks | A phase that needs edge growth specifically, which should enable insertion and record the inviable-recombinant cost (2.5 percent of matings at 10x) |

| D-080 | 2026-08-05 | Proposed technical baseline | **C10.4's gate passes, and it passes narrowly enough that the margin is the finding.** ADR-0022 D1 promoted the genotype-phenotype discontinuity measurement from a metric to a **gate**: two commissioned reviews recommend against developmental encodings, ADR-0019 partially declines that for morphology only, and the concession is that a failed gate takes the parameterized body-plan fallback. Threshold fixed before measuring: a single-locus mutation must leave more than half the body intact, median phenotypic distance below 500 milli, where distance is lattice-occupancy difference over the union with a shared cell of a different module type counting half. Result over 60,000 trials, 11,019 of which reached a growth rule: **median 0, threshold 500 - the gate passes.** But 89.0 percent of those mutations changed nothing at all, against a pre-registered vacuity guard of 90 percent, so the pass survives by one percentage point of a bar I set myself. Stratifying rescues the interpretation rather than the verdict: on bodies of four or more modules, where the metric has resolution at all, the median *effective* mutation moves **351 milli** and is comfortably continuous. The all-sizes effective median of exactly 500 is the metric's own quantization on one-module bodies - a unicell can only score 0, 500, 666, or 1000 - not a discontinuity in the encoding. Zero mutations were lethal. **The carried consequence is not the pass but the silence**: nine in ten morphological mutations do nothing, so morphological evolution under this encoding is slow, and C10.3's campaign must state its expected rate of effective morphological mutation before it runs or it will measure the mutation rate instead of morphology - the Phase 9 lesson (D-079) recurring one phase later | A growth grammar whose mutations are more often effective, which would need the gate re-measured because a less silent encoding is also a less continuous one |
| D-081 | 2026-08-05 | Proposed technical baseline | **Two design reversals in Phase 10's first slice, both found by measurement rather than review.** (1) **Regulatory loci are codominant; an earlier version made half of every diploid growth program inert.** `rules_of` took the first haplotype's copy of each `homology_id` and discarded the second, so every regulatory locus on haplotype 1 did nothing and mutations landing there were invisible - caught only by C10.4's own guard against measuring too few effective mutations. Every other locus type blends its alleles by dominance, but a growth rule has no scalar to blend: averaging two condition codes names a third rule neither parent carried. Both alleles now express, which is what a discrete-valued heterozygote does biologically, ordered by `(homology_id, haplotype)`; identical alleles collapse so a homozygote and a hemizygote grow the same body, without which gene dosage would silently become the dominant morphological signal. (2) **Nothing is a required module type by default.** Requiring a digestive module made 84 percent of randomly drawn growth programs non-viable for one reason, and a body with no gut is not *invalid*, it is *unfit*. Encoding that as a validity rule decides by fiat what ecology decides by starvation, before selection ever sees the organism, which is the authored-outcome failure ADR-0012 and ADR-0018 exist to prevent. Non-viability is now reserved for bodies that are structurally impossible - empty, disconnected, overlapping, out of bounds - and the observed rate fell from 84.4 percent to **zero**. The mask stays configurable because a later phase may want a genuinely structural requirement | A module type that is structurally rather than ecologically necessary, which would be a real use of the mask |

| D-082 | 2026-08-06 | Proposed technical baseline | **Morphology wired into the world through the same narrow seam Phase 9 used, and for the same reason (D-072).** Enabling `morphology` replaces exactly one thing - **how the phenotype is computed**, from a grown body rather than from trait genes - and nothing else. Movement, feeding, pairing, contest, and physiology all read `Phenotype` and cannot tell which produced it. If morphology also rewrote the tick, C10.3's "morphological change has consequence" and C10.6's "the ecology is still stable" would be comparisons between two different simulations. **Three trait genes are retired and only three** - body scale, speed potential, sensor range - and their IDs are never reused: they keep their slots, keep being inherited, and stop being read, exactly as `PlasticityGenes` does before Phase 11. Derived values map into the *existing* clamps so a module-grown organism is interchangeable with a gene-derived one everywhere downstream. **Bodies are derived and never stored**: the save carries only the developmental counters, and a restored world regrows every body from its genome. That is the D-065 trap's twin and the difference is worth naming - the biome map was documented as derived and was *not* a pure function of saved state (it was a classification cached on a cadence), whereas a body is a pure function of a genome and the genome is saved. C10.7's coupling binds at pairing: a child whose expressed network needs more nodes than its neural tissue supports is **refused and counted**, never trimmed, because a trimmed network is one no genome encoded | A phase that needs incremental ontogeny (Phase 14), which makes the developmental clock real state and changes what the save carries |
| D-083 | 2026-08-06 | Proposed technical baseline | **Energy capacity is derived from tissue mass plus storage, not from storage alone, and not from a config floor.** The specification says `energy_max = sum over storage modules`. Taken literally that gives a storage-less body a capacity of **zero**, so every founder is instantly over its own limit and the world cannot start - which is what happened. The first fix was a config floor, and that is worse than the problem: a magic constant standing in for physics. Capacity is now `mass * TISSUE_CAPACITY_PER_MASS + storage capability`, which is both physical - tissue really does hold energy, and a bigger organism really does hold more - and free of the constant. Calibrated so a founder (one digestive module at unit scale, mass 800) lands at exactly 12,000 milli-EU, the same `energy_max_milli` a schema-2 organism gets, so a morphology world starts from identical energetics and diverges only as bodies do. This also fixes a real asymmetry: without it, a storage module cost mass and upkeep and conferred nothing measurable, making an entire registered tissue type strictly deleterious - an authored disadvantage that would have quietly excluded a region of the morphospace | A phase that separates storage capacity from tissue capacity as an evolvable trade, which would need the coefficient swept rather than calibrated |
| D-084 | 2026-08-06 | Proposed technical baseline | **The genome2 and physiology config validations were dead in every world without contest, for three phases.** They had been appended to `validate_contest`, which early-returns when the contest section is disabled - so every genome2 cap check and every physiology range check was silently skipped in most worlds, including **every Phase 8 and Phase 9 campaign**, none of which enabled contest. Nothing failed, because a skipped validation looks exactly like a passing one; it surfaced only when Phase 10's own "morphology requires genome2" check was appended to the same place and its test refused to pass. Relocated into a `validate_subsystems` that `validate` always calls, with a regression test asserting a zero cap is refused **with contest disabled**. No prior result is invalidated - the campaigns used in-range values throughout - but the guard that was supposed to prove it was not running. The general lesson is the one D-075 and D-077 already record in the codec: **a check appended to the nearest function is a check whose preconditions nobody re-read** | A fourth subsystem appended to the wrong validator, which the new regression test now catches for genome2 and physiology but not automatically for a new section |

| D-085 | 2026-08-06 | Proposed technical baseline | **A derived phenotype needs its reference point calibrated to the founder, or enabling the derivation is a systematic tax. Learned three times in one phase.** The first C10.3 campaign killed **29 of 30** morphology worlds, and it was not an ecological finding: a founder was pinned at three clamp extremes at once - basal cost 1600 (maximum), speed 500 (minimum), sensor range 4000 (minimum) - against a trait-derived organism's 954, 1671, 7898. Two causes. (1) **The founder body was a single digestive module**, which is a valid body and a hopeless organism: no motor means zero thrust and the speed floor, no sensory module means the sensing floor. The founder program now grows gut, motor, and sensor - minimal but *functional*, the standard Phase 9's founder network already met. Three modules is a floor rather than a preference: each is the minimum for a derived attribute to be anything other than its clamp. (2) **Derived quantities were mapped onto the clamps with whatever constant looked dimensionally plausible** - basal cost against the world's per-organism cost *rate*, which is not the same scale as a sum of module upkeep. Every quantity is now a ratio to the founder body's own derived values, computed from the founder program rather than hard-coded, so changing a registry coefficient re-centres the phenotype instead of silently biasing it. A founder now lands at basal 1000, intake 1000, scale 1000, speed 1500. This is D-083 (energy capacity derived literally from storage gave a storage-less body zero capacity) for the third time in one phase | A phase that adds a module type conferring a *new* derived attribute, which needs the same calibration and the same test |
| D-086 | 2026-08-06 | Proposed technical baseline | **Three further defects the campaign exposed only by its numbers disagreeing with each other.** (1) **The morphology *config* was never encoded in the snapshot codec**, so every restored world came back with morphology disabled, no bodies, and an empty census - which the analysis reported as "no organism was mature". This is D-065 exactly, one phase later, and `config_round_trip.rs` exists to catch it but predated the section and therefore perturbed nothing in it. **A round-trip test only defends the fields it actually perturbs**, which makes "add the field to the perturbation list" part of adding a config section rather than a follow-up. (2) **The fixed-morphology control was not fixed.** `regulatory_enabled` gated point mutation only, while duplication and deletion select runs of loci without knowing their kind - so growth rules were duplicated and deleted and the control diverged in **21 of 30** worlds. Excluding regulatory loci from run selection would change which loci the other operators reach and raise their effective rate on everything else, making the control a different experiment; the program is snapshotted and restored around mutation instead, so every draw and rate is identical and only morphology is frozen. (3) **The analysis used `.ok()` on restore**, turning a restore failure into "no data" - indistinguishable from a real null. It now fails loudly and cross-checks the restored world's morphology against the manifest | A fourth per-organism subsystem, which needs its config in the round-trip perturbation list and its control checked for operators that reach past the obvious gate |
| D-087 | 2026-08-06 | Proposed technical baseline | **C10.3 not met, and the clause that fails is not the one that matters. Morphology has consequence; it does not spread.** Conjunctive over divergence, consequence, and persistence, all fixed before the run. **Consequence passes 26 of 30**: among the morphological variation that exists, body size predicts reproductive success at a median Spearman of +132 milli against a within-world permutation null of +47, range +5 to +285 - and under the frozen control the same measurement is undefined in 30 of 30, because one body plan leaves nothing to correlate. The null is a shuffle of bodies against reproductive records, preserving both marginals and the whole age structure and removing only the pairing, which is the confound an unpaired comparison cannot address. **Divergence fails 0 of 30**: median module count is the founder's 3 in every world while the *mean* is 4.65 and the median world carries **33 distinct bodies**. More than half the population keeps the founder shape and a minority grows to as much as 18 modules. Persistence is gated on divergence and therefore also fails. This is the fixation-scale problem D-079 recorded for C9.1 recurring on a different mechanism: at 89 percent silent morphological mutations (D-080), nothing reaches half a population in 60 generations. **C10.6 is met** - population 22/30 and lifespan 28/30 within 25 percent or better, both TOST-equivalent, zero extinctions in 90 worlds - so evolvable morphology costs about a tenth of the population and a tenth of the lifespan against a frozen body plan, which is real, reported, and comfortably inside tolerance. The rule is not revised after the fact; what is recorded is that "divergence from the founder distribution" was operationalized as a median shift, and a distributional statistic would have asked a better-posed question | A phase whose criterion is phrased on a population median, which should first state whether it means a fixation-scale claim and set its rate accordingly |
| D-088 | 2026-08-10 | Proposed technical baseline | **C9.6's missing half closed: structural rejections now event as well as count, and the event schema is matched exactly rather than leniently.** The criterion asks a cap to reject, count, *and* event; the counters and the checksum had it and no `EventKind` carried it, so a cap that bound was invisible to every analysis that reads the log rather than the snapshot. Three choices worth recording. (1) **Every typed rejection is evented, not only `RejectReason::Cap`.** The criterion's wording is cap-specific, but the counters it points at are per-class, and a log carrying a strict subset of the counters cannot be reconciled against them - which is the only assertion that makes the event worth having. The reason field keeps the cap subset filterable. (2) **`mutate` returns a fixed-size, allocation-free `MutationReport` and the caller events it**, rather than taking an event sink. Each of the five operators is attempted at most once per reproduction, so five slots is a bound by construction, and `structmut` keeps knowing nothing about the world or the log. (3) **`EVENT_SCHEMA_VERSION` goes 3 to 4 and the decoder keeps rejecting any version but the current one.** The increment is additive and could have been read leniently, but a leniently-read version 3 log reconstructs `structural_rejections_total` as **0**, and "this log predates the event" and "no rejection happened" are opposite conclusions a zero cannot distinguish - trap 12 in the evidence-traps list, arriving through the codec instead of through an analysis. Nothing in the repository breaks: there are no archived `.alev` files, and the eight archived manifests only *record* `event-schema-version 3` rather than being validated against it. Also folded in, because it is the same defect one layer down: `MutationCounters`' totals and `hash_into` were hand-maintained field lists, so a ninth counter would have been silently outside the state checksum the struct's own doc comment promises it is inside. All three now derive from one `..`-free destructuring (D-077) | A campaign whose event logs must outlive a schema increment, which needs a real read-migration path and an explicit "absent" rather than a reconstructed zero |
| D-089 | 2026-08-10 | Proposed technical baseline | **C9.7 closed, and the clause that could not be closed as written is the finding. Three tests named for permutation were proving nothing.** The Phase 9 fixture now exists - config `0x9abc0cd47914127f`, state `0x5f0c4e95e4f5170f`, 500 organisms, seed `0x5eedcafef00dbeef`, **8,000 ticks** - with `scripts/verify-phase9-determinism.sh` running two clean processes, comparing them, and additionally `grep`-pinning both literals, which `verify-phase1`/`verify-phase2` never did. **The horizon is the interesting number.** At the Phase 1/2 horizon of 500 ticks a Phase 9 fixture pins *nothing* about the phase: `maturity_age_ticks` is 600 and founders spawn at age 0, so there are **zero births**, zero structural mutations, one distinct structure. Measured sweep at the fixture's own seed: 500 ticks 0 births, 1,500 ticks 15 births 0 duplications, 3,000 ticks 36 births 1 point mutation and still 0 duplications, **8,000 ticks 124 births / 8 structural applied / 2 duplications / 3 distinct structures**. 8,000 is the first horizon at which meiosis, structural mutation, the refusal path and compaction have all actually run, and the fixture test asserts every one of those counts so it can never silently become a control. **Rule 4's required test cannot be executed as written.** It asks that "permuting the internal storage order of a saved population and restoring it produces identical checksums", but `World::from_state` fails closed on ids that are not ascending, and a *complete joint* permutation followed by the id-sort needed to get past that check is **the identity on the record** - so the test as specified is unfalsifiable. Discharged instead by three tests that each assert something: a positive control proving the permutation harness is exact and saying outright that it is not the evidence; the evidence, which rotates **each per-organism array on its own** and requires the world to refuse, checksum differently, or diverge within 200 ticks - 23 arrays inventoried by `..`-free destructuring, **18 detected, 5 asserted constant** so "not noticed" can never mean "carried but never read"; and the fail-closed control. `determinism-extensions.md` Rule 4 is amended to say this rather than leaving a requirement that reads as satisfiable and is not. **Three sibling tests were vacuous for one shared reason**: each did `vec.reverse()` then `sort_by_key` on a key that was already distinct and ascending, and a stable sort on distinct keys is the exact inverse of `reverse` - so `controller2`'s and `develop`'s "permutation" tests compared a value with a copy of itself, while `phase7_contest`'s permuted nothing at all and was a save/restore test under a misleading name. The first is replaced by a genuine layout difference (the same locus set split across chromosomes differently, which `validate_structure` permits because it requires ascent only *within* a chromosome); the third is renamed; the second is **deleted rather than replaced**, because at that level there is nothing left to permute but the canonical form itself. Recorded there and not asserted: reversing the fixture's rule order grows an identical body, so whether `rules_of`'s sort is load-bearing or belt-and-braces is **open**, and claiming either would have been a second unfounded assertion | A layout refactor that reorders loci within an organism, which is what the compaction tests are a regression guard against and which nothing does today |
| D-090 | 2026-08-10 | Proposed technical baseline | **Per-class mutation and development counters reach the manifest, and `render_run` was the D-077 defect one layer out.** The manifest collapsed 13 `MutationCounters` fields to two sums, in which a cap rejection and a self-loop draw are the same number, so a campaign run with `output snapshots off` could not answer "did a cap bind" - the Phase 9 analysis worked around it by decompressing every world's snapshot and rebuilding a `World` just to read three counters. Now carried as `Option<MutationCounters>` and `Option<DevelopCounters>`, **nested rather than flattened so the compiler enforces completeness**, and `None` rather than zeroed when the subsystem is off, because absent and zero are opposite conclusions. `MANIFEST_VERSION` deliberately does **not** move: Phases 8, 9 and 10 all added columns without bumping, the eight archived manifests say version 1, and a test now asserts all eight still parse with a floor on the file count so an emptied directory fails rather than passing vacuously. The defect worth recording is that **`render_run` was a 41-argument field-access `format!`** while `parse_run` was a `..`-free struct literal - so a field added to `RunResult` broke compilation *in the parser only*, and could be satisfied there with `number("x").unwrap_or(0)` while the renderer silently dropped it, with `manifest_round_trips_exactly` still green because its fixture disables genome2 and morphology and every affected column is `0 == 0`. That is D-077's shape exactly: a compiler nudge in the wrong file plus a test that is vacuous for the field in question. `render_run` is now exhaustive destructuring, and the same treatment was applied to `DevelopCounters::total_nonviable`/`hash_into` (hashed byte order unchanged, pinned by a test built from the pre-refactor list written out literally). Each new test was verified by injecting the defect it exists to catch and observing it fail | A counter that the manifest deliberately omits, which under this pattern must be bound and named rather than left out by absence |
| D-091 | 2026-08-10 | Proposed technical baseline | **The snapshot loader failed open on hostile input, in five sections, since Phase 6.** Every allocation bound read `count.checked_mul(size) > Some(body.len() as u64)`, which looks right and is exactly backwards for the case it exists to catch: `checked_mul` returns `None` **precisely when the count overflows**, and `None > Some(_)` is `false` under `Option`'s derived ordering, so an overflowing count passed the guard and reached `Vec::with_capacity` - a capacity-overflow abort rather than a typed rejection. Present in climate, contest (twice), physiology and schema-2, and inherited by the new learn section. Verified empirically: `u64::MAX.checked_mul(8)` is `None` and `None > Some(100)` is `false`. All eight sites now route through one `allocation_fits(count, per_item, extra, body_len)` whose `None` arm is `false`. **`persistence.rs`'s 2,000-flip corruption sweep never found it**, for two compounding reasons worth recording: a handful of flipped bits does not produce a count anywhere near 2^61, and a panic **aborts the sweep** rather than being counted as a rejection - so the sweep could not have reported it even if it had hit it. The test that found it patches each count-led section's count to `u64::MAX`, `u64::MAX/8` and `body_len`, reseals both CRCs, and carries a `seen == COUNT_LED` guard so it cannot pass by iterating over nothing. A fuzzer that mutates *structure* would have found this; one that mutates bytes could not | A section whose record size is not constant, where a single multiply is not the right bound at all |
| D-092 | 2026-08-10 | Proposed technical baseline | **Phase 11's kernel is built and five of its eight criteria are met; C11.1 and C11.2 are recorded NOT MEASURED, which is not the same as unmet.** Met: C11.3 (save/restore bit-identical with nonzero deltas, corruption injected as a *legal* in-clamp value so the restore must accept it and then matter), C11.4 (birth reset by construction - `push_organism` takes no initial-state parameter), C11.5 (10^6-tick trace bit-identical across clean processes), C11.6 (**10^6 ticks, 18,971,594 updates, 18,970,462 milli-EU charged, ledger exact**), C11.8. C11.7 is partial: the snapshot budget is measured at both tiers (12 bytes per plastic edge + 8 per organism; 0.4 percent of a snapshot at evolved levels, 21 percent at the provisional cap of 32, so **the cap rather than the representation is what decides the budget**), and `learn` p50/p95 are measured across plastic-edge fractions - but checkpoint stall is reported as synchronous encode time, not through `AsyncCheckpointer`, which is a gap and not a substitution. **C11.1/C11.2 are not measured because four things do not exist**: scripted-intervention machinery, a relocatable resource patch (resources are a per-cell scalar field), per-organism action counting, and a neutral marker locus. Recording them unmet would claim a null nobody ran. Three calibration findings sit against them. **The shipped per-tick plasticity cost is zero**: the debit is `cost_milli_per_s * dt_ms / 1000` in whole milli-units, so the default 2 at `dt_ms = 100` truncates to **0** and "the number of plastic edges is under selection" is false as configured - D-085's shape, an uncalibrated derived quantity, for the fourth time. **The cost is also coarsely quantized**: 10 milli/s is the cheapest rate that charges anything at all. And **evolved plasticity is essentially absent** - 1 plastic edge across 500 organisms and 7 across 1,999 after 30,000 ticks, and the founders' 400 plastic edges reach **zero** over 10^6 ticks while 18.9 million updates accumulate along the way | The intervention machinery, which C11.1 needs and which Phase 12's mutable-world specification already defines the representation for |
| D-093 | 2026-08-10 | Proposed technical baseline | **Two more tests that would have reproduced faithfully while measuring nothing, both caught by their own guards rather than by review.** (1) **C11.5's determinism trace was silently a control.** Its input was bound to `energy_fraction`, which is constant in a zero-cost world, so the network reached a fixed point and the learned value reached an equilibrium where decay exactly cancels the delta: `mean_abs_learned_milli` read **964 at 10^4 ticks and 964 at 10^6**. It would have passed "bit-identical across clean processes" forever - the criterion's actual words - while demonstrating that a constant produces a constant. Rebound to a monotone input it reads 100 and 171, and the verify script now **refuses the run if the two horizons agree**, which is the assertion that makes the fixture about accumulation rather than about reproducibility. (2) **C11.6's 10^6-tick world went extinct**, and the population guard is what said so; the ledger would otherwise have been "exact" over a million ticks of an empty world (trap 1, the same shape as A5.3's event-log test). It was not the plasticity debit - the world died with and without it - so the run now mirrors C10.9's known-viable ecology rather than inventing a thinner one, keeping it a test of exactness over duration rather than a re-derivation of carrying capacity. Related, and fixed: `plasticity_anomalies_total` summed **faults and clamp saturations**, which mean opposite things - a saturation is the clamp doing its job and is expected in any run where anything learned, while a fault should be unreachable and is a bug report. Measured 0 faults and 3,908,692 saturations in the million-tick run, so the total would have read as alarming while nothing was wrong. Now reported separately, which is D-074's lesson applied before the counter had a chance to become noise | A third integrator whose "anomaly" total mixes an expected condition with a bug signal |
| D-094 | 2026-08-10 | Proposed technical baseline | **The plasticity charge carries its remainder; `lifesim-plasticity-v2`.** v1 computed `plastic_edges * (cost_milli_per_s * dt_ms / 1000)` and discarded the sub-milli remainder every tick. Every other per-tick cost in the kernel truncates identically and is right to - they are charged once per organism against a large number - but plasticity is charged **per edge**, so the same truncation lands on a small number many times over, and at the shipped rate of 2 milli/s with `dt_ms = 100` it landed on **exactly zero**. A plastic edge was free; 10 milli/s was the cheapest rate that charged anything at all, a tenth of basal per edge. **The only expressible prices were "free" and "ruinous", and the interesting regime - many cheap plastic edges - had no price at all**, so C11.2 could not answer its own question in the direction that matters. The debit is now `(edges * rate * dt_ms + remainder) / 1000` with the remainder carried per organism, so the total charged is under one milli behind the true cost at any instant. The remainder is lifetime-accumulating, so by Rule 7 it is integer, by Rule 8 it is saved and checksummed, and it resets at birth with the rest of the learned state - a child inherits no part of its parent's bill. A restored remainder at or above 1000 is a whole milli that was never charged and is **refused rather than normalized**, because normalizing forgives it silently. This is a behaviour change, so under Rule 9 the policy string moves rather than being redefined, and the Phase 11 fixture moves with it: config `0xa1ad80da0cf6790e` to `0xae34cd2b6f7a3e13`, state `0xe6541fee2698286e` to `0x53b354bd94e82bcf`. The trace's own `mean_abs_learned_milli` is unchanged at 171, because that world zeroes every energy cost - so the fixture moved for the policy string and the new hashed field, not for the arithmetic, which is the expected signature of this change | A cost charged per unit of something numerous, which should ask whether its truncation lands on a small number many times over |
| D-095 | 2026-08-10 | Proposed technical baseline | **Three testing rules adopted as standing obligations, and the third is made structural rather than remembered.** (1) A load-bearing test is not finished until the mechanism has been broken and the test watched to fail. (2) A decode-path test patches declared counts and lengths to adversarial values and reseals the CRCs; bit-flipping tests framing, not bounds, and the 2,000-flip sweep demonstrably could not find D-091. (3) **The config round-trip sweep is driven from `FIELD_NAMES`.** The third is the one worth the code: a hand-maintained perturbation list defends only the fields somebody remembered, and a field left at its default compares default-to-default and round-trips "successfully" whether or not the codec ever wrote it. That exact defect landed three times - D-065 (climate, origin, contest, physiology), D-086 (the whole morphology section), and the plasticity gate, whose loss silently turned a treatment run into a control across any checkpoint, and which also revealed that the *entire* genome2 section had never been perturbed, leaving `regulatory_enabled` - C10.3's whole control - undefended for two phases. `crates/sim-experiment/tests/config_field_coverage.rs` now walks the registry, perturbs every entry, round-trips through the real codec and asserts each field returns, with a guard that the perturbation actually changed the value (or the comparison is vacuous) and a floor on the number swept (or an emptied registry passes). It lives in `sim-experiment` because that is the only crate depending on both the registry and the codec; putting it in `sim-persist` would invert the layering. Verified by dropping two unrelated fields from the codec and watching it name each one | A config field that is deliberately not settable by a campaign, which this sweep cannot see and which therefore needs its own stated reason |
| D-096 | 2026-08-10 | Proposed technical baseline | **Phase 12's plan still argued the pre-reversal ordering, and one of its criteria referenced a fixture that does not exist.** The plan carried a section headed "Why This Is After The Social Channel", listed Phase 13 as a prerequisite, and closed with C12.8 requiring that a disabled world "reproduces the Phase 13 fixture exactly". `docs/19-implementation-roadmap.md` records the opposite and ADR-0022 A2 triple-sources it: **11 before 12, artifacts precede signalling**, because an artifact left behind is a transmission channel that requires no perception of conspecifics at all - `cumulative_culture` 1.2 puts persistent artifacts inside the *minimum viable* transmission system, `artifacts` 1.7 puts social transmission at step 10 of 12, and `social_organization` 1.1 puts stigmergic cooperation before communication. The plan text was never updated when the roadmap was. Corrected in the plan rather than worked around silently, per the rule that a contradiction with a recorded decision is resolved explicitly. Three consequences. **C12.3 is measurable in this phase**: composite objects accumulating is an artifact-mediated question, and stigmergy is the transmission channel it needs. **Nothing in the phase may be deferred on the grounds that Phase 13 does not exist.** And **C12.8's obligation is broader than written, not narrower**: the fixtures a disabled world must preserve are Phase 1 `0x1e3158a26afd3b39`, Phase 2 `0xff9dfcff5dffbf42`, Phase 9 `0x5f0c4e95e4f5170f` and Phase 11 `0x53b354bd94e82bcf` - four, not one. Recorded before any Phase 12 code exists, so the criterion cannot be read as having been relaxed after the fact | A phase plan whose ordering argument predates a roadmap reversal; the remaining plans should be checked against the roadmap before each is entered |
| D-097 | 2026-08-10 | Proposed technical baseline | **The mutable-world half was verified by mutation rather than by reading its own report, and one decode-time invariant turned out to be defended by nothing.** `specifications/mutable-world-state.md` makes sortedness and uniqueness of the modification set **decode-time invariants**, and deleting the `order_violation` guard from `from_state` left **every test green**. So did deleting the `bounds_violation` guard. The reason they hid is defence in depth *without discrimination*: a disordered set also produces a different composed checksum, so the far guard still refused the save and the world was still fail-closed - but the existing tamper test asserts `matches!(err, ComposedTerrainChecksumMismatch | StateInvalid)`, so it could not tell which layer fired and both deletions were invisible to it. That is the same shape as Phase 11's restore guards, where two independent refusals meant removing either one changed nothing a test could see. Closed by two tests that pin the **near** guard by its message: one swaps two adjacent entries wholesale, so the multiset of overrides is unchanged and the defect is disorder and nothing else; the other moves the last entry's cell past the end of the map, so the set stays ascending and the order guard cannot be what fires. Both were verified by re-deleting the guard and watching the specific test fail. The diagnostic is what is being defended, not the refusal: the order check exists so a reader is told the set is disordered rather than being sent to look at a checksum. **Two mutations that survived and should have**, recorded so the next reader does not chase them: the 3-to-4 migration's `state.worldmod = None` and `composed := baseline` lines are unobservable, because a worldmod-less file stores neither field independently - the composed word is written *only* alongside a modification section and is otherwise resolved to the baseline on read, which is provably its value for an empty set. Those two assertions in the migration test are tautologies; the rest of that test - state equality, world equality, and a 200-tick divergence check against a format-3 load - is real | A guard whose only test accepts a set of errors rather than the one it is supposed to produce; the pattern is worth grepping for |
| D-098 | 2026-08-11 | Proposed technical baseline | **`mean_abs_learned_milli` is the wrong summary for a rare phenomenon, and it was read as "nothing learned".** Phase 11's confirmatory findings state that "the mechanism executed a billion times and moved no weight by as much as one part in a thousand", resting on `mean_abs_learned_milli = 0` in all 30 Avar worlds. A descriptive census of the same 120 snapshots (`experiments/results/phase11-conjunction-census.txt`, `lifesim conjunction`) shows the mean is arithmetically correct and the reading is not: **25 of the arm's 48,119 plastic edges ended with a nonzero learned weight, in 14 of the 30 worlds, the largest at 15,054 Q16 = 230 milli** - 2.9 percent of `LEARN_LIMIT_Q16`. `sum(abs) = 139,116 Q16` over 48,119 rows truncates to 0 milli, so a population where 25 edges learned and one where none did report the same number. This is D-074's shape - a summary that makes a rare signal unreadable - applied to the one metric the phase used to answer "did anything actually learn". Two further numbers from the same census correct the same paragraph: `plasticity_updates_total` is `total_evaluated`, so **95.4 percent of the "billion updates" were `StepKind::Static`**, the early return at `plasticity.rs:375` for a flagged edge whose rule is 0, and only 3,147 of the 48,119 plastic edges carry a non-static rule at all. Neither correction changes C11.1 or C11.2, which stay UNMET on their pre-registered thresholds; both change what the null is *about*. **Not fixed in the kernel by this change**, deliberately: the census reports the count, the maximum and quantiles over the nonzero rows from the snapshot, and adding a counter to `Metrics` is a separate change with its own reporting-surface consequences. Recorded so that the next reader of a zero mean asks for the count | Any report that cites `mean_abs_learned_milli` as evidence that nothing learned; and a decision on whether `Metrics` should carry `learned_edges_nonzero` beside the mean |
| D-099 | 2026-08-11 | Proposed technical baseline | **Phase 11's null is a null about reachability, and the interior of the path to plasticity is a plateau with a moat on it.** A descriptive census of the confirmatory campaign's own 120 snapshots (`lifesim conjunction`, `sim-analysis::conjunction`, `experiments/results/phase11-conjunction-census.txt`) counted the conjunction a nonzero learned delta needs on one edge locus. It is **four** conditions, each behind a *different* one of the seven point-mutation targets `structmut.rs:631` draws - `EDGE_FLAG_PLASTIC` (target 1, a toggle), `rule_id != 0` (target 2, a re-draw), `eta > 0` (target 3), and a nonzero coefficient (target 4) - with two corrections to the obvious reading: `RULE_OJA` reads no coefficient (`plasticity.rs:388`) so it needs three, not four; and rules 3 and 4 need a **fifth** condition, a `modulator_node` naming a node whose role is `Modulatory`, or `learn_phase` multiplies their update by a hard `0.0` (`world.rs:3953`, `controller2.rs:253-264`). **9 of 684,370 Avar edge alleles satisfy all four** - 13 per million, in 4 of 30 worlds - against a depth histogram of 524,025 / 136,261 / 22,602 / 1,473 / 9 at depths 0 to 4. Diploid expression is more permissive than either allele (`blend_plasticity` takes `rule_id` from haplotype 0 and blends `eta` by dominance while the flag is the OR), which is why **59 of the 48,119 expressed plastic edges** clear it and why the phenotype existed at all. **The gradient argument is settled by the code and then measured.** Every incomplete state on the path computes bit-identically to the founder: rule 0 returns before reading eta (`plasticity.rs:375`), `eta = 0` sends every rule form to `to_q16(0.0) = 0`, and `effective_weight(w, 0)` is exactly `w` for an in-range genome weight, so evaluation is unchanged (`controller2.rs:540-547`). But `learn_phase` charges **every** plastic edge, rule-0 included (`world.rs:4008`), so a flagged incomplete edge is not neutral - it is 0.2 milli-EU per tick against a 10 milli-EU basal, 2 percent of basal metabolism for a gene that provably does nothing. Measured: the Avar arm paid **221,410,876 milli-EU** for plastic edges, of which 95.4 percent bought rule-0 no-ops. There is therefore **no monotone non-negative path from the founder to the phenotype**, and the C11.2 null ("plasticity spread at exactly the drift rate") is what that landscape predicts rather than a statement about the value of learning. The waiting time confirms the scale: at the campaign's `point_q16 = 65535`, one birth delivers a point mutation to a named edge locus with probability 1/45.76, an exact 16-state chain over the four conditions needs ~24 hits at that locus, so ~1,100 births in one line of descent - against a mean `max_ancestry_depth` of 63.7 generations, which delivers 1.4. **Short by a factor of about 17.** Two internal checks make the census readable: the depth histogram partitions the alleles exactly (marginal sum equals the depth-weighted sum, 185,920 in Avar), and re-compiling every genome at the world's own budget reproduces `plastic_edges_total` on all 120 worlds. The census is labelled a census on its first printed line and has no threshold, no null and no verdict, because it was run with the outcome known | A revision that starts the founder at depth 3 with only the flag left to evolve, or any change to `point_q16`, the founder `PlasticityGenes`, or the per-edge cost; and any claim that Phase 11 measured the value of lifetime learning rather than its reachability |
| D-100 | 2026-08-11 | Proposed technical baseline | **C11.1's matched control is not age-matched, and the age offset on its own clears the criterion.** `boundaries` puts the control at `event + relocate / 2`, so a control window pair lies 1,000 ticks later in **every** organism's life than the event pair it is matched to - and at a different phase of the epoch, immediately after the event pair's own post-window rather than straddling anything. The confirmatory findings file already named this, having found it with the campaign's own zero-magnitude control, and concluded that it made the two-sided column unreadable but left the decision alone. **The stronger statement is now measured**: the offset alone can produce a *pass*. `the_matched_control_is_not_age_matched_and_the_offset_alone_clears_c11_1` builds a stationary rolling cohort - `AGE_LANES` organisms born every window, each living exactly 24 windows, so the population holds the identical multiset of ages at every sample tick and no world-level quantity moves with the tick - in which nothing whatever happens at the event tick and behaviour is a pure function of age. It reports **`rho = +158` against a null of 30, and `within_lifetime_shift` returns true**, when the window-to-window behavioural change decays with age; `rho = -154` when it grows with age, which is the sign the campaign measured in all four arms; and **`rho = 0` exactly** when the change is age-dependent but has no curvature, which is the control that says the cohort construction manufactures nothing by itself. The mechanism is identified by dose-response rather than by argument: at offsets of 500, 1,000, 2,000 and 4,000 ticks the correlation is **76, 158, 334 and 700 milli**, each doubling of the offset roughly doubling it at ratios of 2.08, 2.11 and 2.10 - close enough to linear over an eightfold range that a control taken symmetrically at `T +/- relocate / 2` would cancel most of it. **The permutation null cannot see any of it**, because every draw shuffles labels over the same age-imbalanced pairs; and the artifact is a fixed effect size while the null shrinks like `1/sqrt(n)` (rho 163 to 139 while the null falls 85 to 15 as the pooled pairs go 240 to 7,680), so it decides *more* worlds the more data there is. At the campaign's ~27,000 pairs per world the null p95 was 6 to 14 milli. **So the directed statistic is not identified**: the pairing turns an age trend of either sign into a signed association at these sample sizes, and this campaign's 0 of 30 depends on which sign its substrate happens to have. **Stated as a limit rather than glossed**: the campaign's `.alac` artifacts were not retained, so the magnitude of the age trend in its own worlds is *not* measured here. What is measured here is the mechanism and its dose-response; what the campaign itself contributes is the Bstat association, which is this statistic with the event removed. **Nothing was changed to accommodate this** - no threshold moved, no decision rule touched, no campaign re-run - because a threshold is never revised after the data is seen; the correction belongs to a future pre-registration and is filed as an open question. The one age-free reading available from the campaign as it stands is already in the report and is null: the seed-paired contrast of `rho` between arms, Avar minus Bvar = +3 milli, 95 percent interval [-1, +8], p = 0.707, in which the offset is common-mode and cancels. **The same pass found the test suite defended less than it looked.** Forty mutations were injected across the window distance, the pre/post pairing, the id-keyed lookup, the matched control, the permutation null, the directed rule and the verdict; **26 were caught and 14 survived**, against a prior agent's report of 28 injected and all caught. Twelve of the fourteen were real gaps and are closed by seven new tests and three extended ones. The worst was that **`summarise` had no test at all** while `outcome.shifted` is exactly what `Verdict::decide` reads as C11.1's treatment count, so the directed rule that two tests pin inside `within_lifetime_shift` was re-decided one layer up where nothing was watching - swapping in `behaviour_associated` there turns the campaign's 0 of 30 into 29 of 30 and is invisible. Also undefended: the **id-keyed lookup**, which the module documentation says the entity id is in the file for, because every synthetic series wrote records in ascending id order with identical ages so slot-keying and id-keying agreed everywhere; **`pairs_of`**, whose seed matching could degrade to nearest-seed; the alignment guard, which for an interval that is a whole number of windows but whose half is not would have been masked by `too_few_individuals` - D-097's near/far shape again; the inclusive horizon bound; both `Verdict` thresholds at their exact boundary; `min_individuals` at its bar; `rho` exactly equal to its null; the analysis seed actually reaching the shuffle; and the full-L1 correlated quantity, which every other fixture leaves as a monotone transform of the locomotion half. **Two survivors are tautologies and are recorded so nobody writes a test for them**: `let mut event = relocate` is redundant with the burn-in floor, which rejects tick 0 for every legal window; and the `.max(0)` in the occupancy total sits behind `window_distance`, which has already refused every negative delta | Any change to the control boundary, `relocate_interval_ticks`, the sample interval or the burn-in, each of which moves the numbers this test pins; and any reading of the `associated` column, or of a positive C11.1 count from this pairing, as evidence about an event |
| D-101 | 2026-08-11 | Proposed technical baseline | **The kernel measurement substrate was re-verified by 23 independent mutations; 12 survived and 8 of those were real gaps.** A prior pass reported 29 mutations all caught. Re-run from scratch against `actioncensus.rs`, its `world.rs` integration, the `.alac` codec and the sampling path: 11 caught, 12 survived. **The `.alac` decode path was the worst of it.** Seven declared-value guards had no test at all - the header's `max_organisms`, the `flags` word, the declared `header_len`, the `build_len` cap, the strictness of the tick order, and both encoder-side bounds - and each survived deletion. The build-length cap is the one that mattered: the existing tests only ever set an over-long `build_len` on a *short* file, where `TooShort` fires whether or not the cap exists, so the assertion passed with the cap deleted. A header declaring 100 bytes **with the 100 bytes actually present and the CRC resealed** decodes cleanly without it. That is standing rule 2's near/far shape (D-097) inside a test written to obey standing rule 2. The tick-order guard is the second: `ticks_must_strictly_ascend` only tested a *descending* pair, so relaxing `tick <= previous` to `tick < previous` survived, and a repeated sample tick would give C11.1 two different "before" rows for one organism at one tick. **In the kernel, two mutations were caught while the test named for the property was not what caught them.** `the_census_stays_in_lockstep_and_a_desync_is_a_typed_failure` ran 600 ticks; in that world the first death is at tick 380 and the first paired birth at **765**, so it checked lockstep over a population that had only ever shrunk, and deleting `push_organism` from the paired birth path left it green. A compaction that keeps the right *number* of rows and the wrong ones also left it green, because equal lengths are not lockstep. It now runs to 1,000 ticks, asserts a birth and a death actually happened, and asserts the locomotion block equals its own organism's age - the only assertion here that can tell a paired row from a mis-paired one. **And the observation-only claim was asserted over a column set that was four-sevenths constant.** A feedback from the census into `intent_speed_milli` gated on the `Attack` count survived all four seeds of `counting_actions_changes_nothing_the_world_computes`, because `Rest` and `Attack` are the two classes whose threshold is *positive* (+0.5 each) while `Eat`'s and `Mate`'s are negative, and an unbound action channel reads exactly 0 - so the sign of a config threshold decides four of the seven columns outright. Two arms added with those thresholds flipped, which fills the columns and catches the mutation. **Four survivors are tautologies and are recorded so nobody writes a test for them.** (1) The schema-1 asexual birth path's `push_organism` is dead code: the census requires phase2 and `apply_intents` requires its absence, so `pending_births` is provably empty whenever the census exists. (2) The census push's position relative to the **birth-time** schema-2 refusal is unobservable, and this was measured rather than argued - four hostile 20,000-tick worlds at 4x duplication and insertion produced **1,022 non-viable refusals, every one of them at pairing time**, and the birth-loop site never fired once. (3) Rewriting the `body_len` cross-check from u64 into wrapping u32 is unobservable *because of* the count cap's value, not because of the code: 10^6 x 44 is far inside u32. That coupling is now an assertion, so raising `MAX_SAMPLE_ORGANISMS` past `u32::MAX / RECORD_LEN` fails a test instead of quietly re-opening the hole. (4) A census feedback of **+1** milli on the speed intent is annihilated by integer division in the movement step; the same feedback at +500 is caught, so the survivor was a property of the mutation's magnitude and not of the test. **The occupancy finding that bounds C11.1 was reproduced independently** on three fresh Avar and three fresh Bvar worlds at the confirmatory config, decoded by a from-scratch reader written against the format comment rather than against `actionlog.rs`. Over 1,175,285 Avar records: `eat == age` and `mate == age` in **100.0000%**, `rest == 0` and `attack == 0` in **100.0000%**, `sum(locomotion) == age` in **100.0000%**. Over 839,154 whole-lived windows, `turn_right` is **93.61%** of locomotion (94% reported), `move_ahead` 5.79%, `turn_left` 0.59%, and exactly **3 of 7 columns take more than one normalized value** - the other four take exactly one. The B arm reproduces it at 93.40%, so the profile is a property of the thresholds and the founder controller and not of plasticity. **So the answer to "which columns could ever have carried a plasticity signal" is: the three heading bands, which are a partition and therefore two degrees of freedom, one of which carries 0.59%.** C11.1's detectable space was effectively one number - the fraction of ticks turning right rather than straight - driven by the founder's single evolved `energy_fraction -> turn` path. `eat` and `mate` could only ever have *fallen* below the age, which requires evolution to drive an output under -0.5 or -0.25 and never happened in 60,000 ticks; `rest` requires an output above +0.5 and never happened; `attack` is structurally zero because the campaign leaves the contest section off. | Any change to `rest_threshold_q16`, `attack_threshold_q16`, `eat_threshold_q16` or `mate_threshold_q16`, each of which moves which columns are constants; any raise of `MAX_SAMPLE_ORGANISMS`; and any future claim that C11.1 observed a behavioural repertoire rather than one heading statistic |
| D-102 | 2026-08-11 | Proposed technical baseline | **The Phase 11 measurement substrate: a config section of its own, snapshot section 14, a checksummed per-organism action census, the `.alac` artifact, and a neutral marker locus.** Decided and built in commit `aaa260e` on 2026-08-10; recorded here in the reconciliation pass, because the campaign that consumed it was pre-registered and run before its design was written down. Five choices, each with a consequence that would otherwise be discovered by a later reader. **(1) `ProbeConfig` is a section of its own rather than fields on `genome2` and `plasticity`, and the reason is fixture preservation, not tidiness.** A field appended to either of those is hashed by every world that already enables them - which is the Phase 9 and Phase 11 fixtures - so a measurement nobody switched on would have moved two fixtures. A section appended last and hashed only when enabled cannot (D-014). It carries two sub-gates, `action_census_enabled` and `marker_locus_enabled`, because a run can want the drift control without paying a histogram per organism and vice versa; a sub-gate on with the section off is **refused**, not treated as off, because a probe that quietly does nothing still produces a *file* - an empty one - rather than an obvious absence. Enabling it starts a new replay lineage, correctly in both halves: the marker changes what point mutation can land on, and the census changes what is stored and checksummed. **(2) Snapshot section tag 14, and the ALIF format version deliberately stays 4.** This follows `SECTION_LEARN`'s precedent rather than `SECTION_WORLDMOD`'s: format 4 was bumped because the logical state gained a meaning (a composed terrain checksum in the header), not because a section was appended, and an absent optional section is readable by every existing build. The decoder still refuses tag 14 in a format 3 file, so a legacy file carrying it is a typed error rather than a section read under a framing that never defined it. Dense, unlike the sparse learn section beside it, because every living organism has an action every tick and a sparse histogram would carry an index next to almost every entry. **(3) The census is world state, so it is saved and hashed - and `reset` therefore moves the checksum.** A lifetime's action counts have no source but the save: they accumulate from intents computed from stored activations, and re-deriving them would need the run replayed from tick zero. That is `learnstate.rs`'s argument verbatim. The cost is stated rather than hidden: a world whose counters were zeroed at tick 5,000 is not the world whose were not. The consequence is that **the sampling path must never call `reset`**, and it does not - the artifact records cumulative rows and a window is the difference of two samples, which subsumes a reset and keeps sampling provably read-only. **(4) A row is a partition block plus an indicator block, and the first design was an instrument that could not vary.** The obvious layout arbitrates one class per organism-tick by precedence. Built that way and measured before being believed, it recorded `[rest 0, ahead 0, left 0, right 0, eat 0, mate 458212, attack 0]` in a 120-organism world over 8,000 ticks - every organism-tick in the `Mate` column, in every seed at every horizon, because `mate_threshold_q16` is negative and an unbound channel reads 0. A top-priority class that is always set makes every other column unreachable and C11.1 would have been measured against a constant: trap 1 arriving through the instrument rather than through the world. So there is no precedence at all. Columns 0..4 partition the tick into rest and three heading bands and sum to the organism's age; 4..7 are independent indicators. `TURN_BAND_MILLI = 125` is the one authored value left. **(5) `.alac` format 1, with identity in every record.** Binary rather than text on an arithmetic argument, not a taste one: one sample is `population x classes` plus an id and an age, so a 2,000-organism world sampled every 500 ticks over 10^6 ticks writes 4 million records - roughly a quarter of a gigabyte as text against 176 MB framed at 44 bytes. The entity id is a per-record field and not an optimization to be removed later: an artifact recording rows in array order would let an analysis line up two samples by index, and index order changes whenever anything dies, so the "before" and "after" of an individual would silently become two different individuals - the exact failure C11.1 exists to avoid, arriving through the file format. `class_count` and `policy_hash` are provenance and refuse a file written under a different `ACTION_CENSUS_POLICY_VERSION` rather than reinterpreting it | A second measurement instrument that wants the probe section, which must add its own sub-gate rather than widening an existing one; any change to the class set or `TURN_BAND_MILLI`, which is a new `lifesim-action-census-v1` successor because a histogram under one convention is not comparable with a histogram under another; and any caller that wants `reset` during a run, which makes the probe boundary part of the replay lineage |
| D-103 | 2026-08-11 | Proposed technical baseline | **The neutral marker locus is matched by mutating on exactly the draws it controls for, and adding it is not free of consequence.** `LocusKind::Marker` (schema-2 tag 6, `MARKER_HOMOLOGY_ID = STRUCTURAL_HOMOLOGY_BASE + 4_500`) is C11.2's drift control, and four properties make it one; all four were verified by 40 mutations rather than by reading the code. **Same per-locus rate**: the marker arm takes the identical `draw(8) % 7` target selector, moves `value` on target 3 (eta's target) and toggles the neutral bit on target 1 (`EDGE_FLAG_PLASTIC`'s target), and the other five targets `return false` with **the draw still spent**. Measured over 120,000 reproductions with both alleles seeded at 0.5, off the clamp so every delta is visible: marker value 781, marker flag 783, eta 1,571, plastic flag 1,569 - value and flag equal to 0.3 percent, and the edge-over-marker ratio 2.012 and 2.004, which is the locus count (4 edge alleles against 2 marker alleles per diploid founder) and not a rate difference. First principles predicts 120000/22/7 = 779. Redirecting the five unused targets onto the marker's own two fields would have made it drift 3.5x faster than the genes it controls for, and "a shift beyond drift" measured against a faster-drifting control is a threshold nobody set. **Same clamp at both bounds**: `.clamp(0.0, 1.0)`, an absorbing boundary rather than a reflecting one - the `genome2.rs` doc calling it "reflected" is loose. At the floor the marker moved visibly on 51.6 percent of draws and eta on 50.3, every visible move upward for both; at the ceiling 48.1 and 49.7, zero upward moves for both. **Same linkage**: the marker sits *between* the two founder edge loci, one merged rank from each while the edges are two ranks apart, because crossover positions are drawn over merged rank and a marker parked at the end of the chromosome would recombine away from what it controls for faster than those genes recombine from each other. This is the best available matching and not a perfect one, and it degrades slightly over a run: Avar ended at 2.206 marker alleles per organism against a founder's 2 (x1.103) and 4.116 edge alleles against 4 (x1.029), because duplicated copies land at a random offset. **Never expressed**: `express_network` matches `Marker` into an empty arm, `express_traits` matches only `Trait`, `develop::rules_of` only `Regulatory`, `compatibility_distance` builds its id set from `express_network`, and `structural_signature` hashes nothing beyond the tag - so a marker allele cannot reach mate choice or structural identity either. **Deliberately not gated on `plasticity_enabled`**, which is the property the whole B arm rests on: gating it would freeze condition B's control while condition A's kept moving, the mirror image of D-086. Measured: with the gate off the edge arm spends all seven targets on `*weight` (10,932 = 7 x 1,562 hits, eta 0, plastic flag 0) while the marker's 781/783 are **bit-identical to the gate-on run**. **The consequence that is not free, and it is stated on the config rather than discovered**: a marker is never expressed but it *is* a locus, so adding one to a genome of `n` loci lowers every other locus's share of the mutational input to `n/(n+1)`. That is not a defect - it is what "mutates at the same rate as the genes it controls for" *means* - but it makes a marker world a different experiment from a marker-free one, which is why the gate exists and why all four of C11.2's arms carry the marker. **The campaign confirms the match end to end**: 26,654/684,370 = 3.895 percent per edge allele against 13,751/366,871 = 3.748 percent per marker allele, a ratio of 1.039, while the raw-count ratio of 1.938 is just the allele-count ratio. Per world the medians are 3.55 and 2.77 percent (Avar) against a between-world marker range of 0.44 to 14.20 percent - a factor of 32 - so the residual is far inside drift noise. **Two honest notes.** `value_milli` truncates to milli before the moved-allele test, so 26,654 and 13,751 are lower bounds; it is symmetric across both sides and is the same truncation that produced D-098 one level down. And the A-arm marker's moved fraction (per-world median 2.77 percent) sits below the B-arm's (4.93), consistent with background selection - the flagged edges are deleterious and the marker is one rank away, so it shares the reduced effective population size. **Cross-arm comparisons of marker quantities are therefore not meaningful**; the marker is a within-arm control only. One documentation error fixed while verifying this: `schema2.rs` claimed the marker's `gene_lineage_id` is what makes "the loci align during meiosis". Meiosis aligns on `homology_id`; `gene_lineage_id` is written by the codec and read by nothing, so a mutation of that line is unobservable rather than defended, and the comment now says so | Any operator that reaches `LocusKind::Marker` beyond point mutation, which would break the draw-for-draw match; a founder carrying more than one marker locus, which changes the dilution factor; and any cross-arm comparison of a marker quantity, which the background-selection asymmetry makes unsound |
| D-104 | 2026-08-11 | Proposed technical baseline | **`lifesim-plasticity-analysis-v1`: a signed statistic against a two-sided null, one drift margin anchored to a mutational step, and one guard where a second would have alibied it.** The analysis that decides C11.1 and C11.2, built in `aaa260e` and recorded here. **The observed statistic is signed and the null is the p95 of the absolute value, on purpose.** `morph.rs` compares the absolute value of rho against the p95 of that same absolute value, because C10.3b predicts no sign; C11.1 does predict one - a relocation is predicted to make an individual's action distribution move *more*, not less - so only a positive correlation can pass while the null keeps both tails. That bar is at least as strict as a one-sided test at five percent and never weaker, and it is what refused the campaign's negative associations in all four arms rather than counting them. The two-sided count is computed and printed beside it as `associated` and **decides nothing**: reporting it is what stops a sign-reversed result being invisible in a report whose rule is directed, and the campaign is the case that vindicates it - 29 to 30 of 30 in every arm including the one where nothing happens at the event tick. **`drift_margin_milli = 25` is anchored, not chosen.** A point mutation's delta is uniform on +/- `point_delta_q16 / 65536` of the value's range, which at the pinned 3,277 is +/- 0.05, so the expected absolute step is exactly 25 milli: an excess smaller than one typical mutational step is not an effect worth naming. The plastic-flag scale is matched to it by construction, because the marker's flag toggles on the same draw as `EDGE_FLAG_PLASTIC`, so 25 milli is 2.5 percent of alleles on both sides. **`genome2.mutation.point_delta_q16` joins `FIELD_NAMES`** for exactly that reason: a campaign that states a bar computed from a constant and cannot pin the constant would have its threshold moved by a later revision of a default - the coupling D-078 removed for Phase 9's caps. The codec had always carried the field; only the registry entry was missing, which is the "visible gap rather than a silent one" standing rule 3 describes, and adding it puts the field under the `config_field_coverage` sweep automatically. **A duplicated `min_individuals` check was removed rather than kept as defence in depth.** `world_shift` already refuses a thin world by name; a second copy of the same bar inside `within_lifetime_shift` would be a guard no test could distinguish from the first - delete either and everything still passes. That is D-097's exact shape, and the choice here is the general rule stated forward: **one guard pinned by its own diagnostic beats two that alibi each other.** `plan` stays in the predicate's signature because the bar it carries is the criterion's and a reader should be looking at the plan. **No variance means undefined, not zero, on both criteria**, and it matters more here than in `morph.rs`: the phase's own substrate measurement found four of seven columns constant in a founder-dominated world, and counting such a world as a failure would let a degenerate controller masquerade as a refutation of lifetime learning. C11.2's `drift_no_variance` separates "the control never ran" from "plasticity failed to keep up with drift", which are different results. Analysis versions stay out of the config hash (D-022); `morph::permutation_p95_milli` is called verbatim rather than reimplemented, so a plasticity report is reproducible from its recorded `analysis_seed` alone | Any change to the class set, the window distance, the pairing, `PERMUTATIONS` or the sample interval, each of which is a new analysis version; a revision of `point_delta_q16`'s default, which moves the anchored margin and must move the stated bar with it; and any future criterion tempted to add a second copy of a guard that already fires |
| D-105 | 2026-08-11 | Proposed technical baseline | **C11.1 and C11.2 are measured and both are unmet, measured nulls - and the campaign that measured them ran at ten times the shipped point-mutation rate, which bounds how far the null generalizes.** `experiments/phase11-c111-confirmatory.campaign`, hash `0x96ed767dbd9060e6`, pre-registered in commit `4b160fe` **before** the run; the campaign file is byte-identical since and no analysis code changed between pre-registration and results. 120 worlds (4 arms x 30 seeds, 6001..6030, no exclusions, preflight refused none), 60,000 ticks, 8 workers, 4,512.9 s, 0 failed. **C11.1: Avar 0 of 30 against a bar of 20, Bvar 0 of 30 against a ceiling of 20. C11.2: Avar 8 of 30, Bvar 0 of 30**, all eight on the plastic-flag scale and none on `eta`, with one extinct Avar world reported `drift_no_variance` rather than as a failure. Seed-paired Avar minus Bvar on rho is +3 milli, [-1, +8], p = 0.707. No threshold was changed after the data. **Two calibrations move this off the shipped defaults and both belong on the result rather than only in the campaign file.** `genome2.mutation.point_q16 = 65535`, **ten times the shipped 6554**, adopted on a control rule fixed in a pilot on disjoint seeds: the neutral marker's alleles must actually move, or the drift control cannot bound drift and C11.2 is undefined everywhere for a reason about the mutation rate - at 6554, 0 marker alleles had left the founder value in the pilot; at 65535, 126. And `patch_radius_cells = 32` with `patch_capacity_scale_q16 = 262144` (4.0), against defaults of 15 and 2.0, on the rule that the schedule must measurably change the world - it does, the E-variable arms carrying roughly twice the population of their seed-matched zero-magnitude controls (median 5,964 against 2,846; 5,653 against 2,716). **So the null is a null at ten times the shipped mutation rate**, and the waiting-time arithmetic scales inversely with it, so at the shipped rate the shortfall to assembling the phenotype would be a factor of roughly 170 rather than 17 - first-order, holding the locus count fixed. **The environment control is a zero-magnitude schedule rather than a schedule-free world**, because relocating a patch trims biomass into a ledgered loss sink and a schedule-free arm would differ in standing biomass for a reason unrelated to learning. **One clause of C11.2's verdict is satisfied mechanically and is recorded rather than left implicit.** With `eta` and `EDGE_FLAG_PLASTIC` frozen by the mutation gate, both B-arm excesses are always at most zero and `selected_over_drift` is unreachable by construction, so "the control stays strictly below the ceiling" could not have been contradicted. It did not matter, because the criterion failed on its treatment count alone (8 < 20); it would have mattered had Avar reached 20, and the criterion would then have read "met" with a control incapable of refuting it. This is "a control bound to a constant is not a control" one rung up from where the findings file caught the same shape in the `plastic_excess_milli` contrast. C11.2's live control is the within-arm neutral marker (D-103), which is matched and does move. **The phase's Risks table names this null as its single most likely failure, and it happened - but the row's wording is wrong about the mechanism.** It predicts "plasticity is selected to zero"; the census (D-099) shows plasticity was never assembled. The revisit conditions on D-023, D-025 and D-035 are phrased on "selected to zero" and are therefore **not** triggered, and reading them as triggered would draw the wrong inference about Phase 13. The same census removes the last Risks row's stated partial mitigation: rules 1 and 2 being ungated gives the search no gradient, because an edge with rule 1 and `eta = 0` executes the same instructions and writes the same bytes as one with rule 0 | Any re-measurement of C11.1, which must first take the control-boundary correction in `docs/21-open-questions.md`; any run at the shipped `point_q16`, which is a different and harder regime and must not be compared with this one as if matched; and any reading of C11.2's B-arm zero as an informative control |
| D-106 | 2026-08-11 | Proposed technical baseline | **`lifesim-plasticity-analysis-v2` changed the statistic as well as the null, and the code, the doc comments and the pre-registration all said it did not.** D-100's correction shipped in `9f1c340` under the banner "restrict the randomisation, do not touch the statistic", with **"the observed statistic is unchanged"** in bold in section 2 of `experiments/phase11-c111-agematched-preregistration.md` and repeated verbatim in `morph.rs`, in the `PLASTICITY_ANALYSIS_VERSION` doc and at the call site in `world_shift`. It is false, and the pre-registration's own section 6 table always contradicted it: the design excludes observations sitting in a single-label stratum from the **observed** statistic as well as from the null, and on D-100's own rolling cohort the observed rho therefore goes **158 to 0**. At the widest dose the exclusion removes **384 of 672** observations. **Which half does the work was never measured and is now measured**, on the stationary cohort where change decays with age at `R = 2,000`: neither, 158 against 30, **passes**; stratified null only, 158 against 162, refused by four milli; exclusion only with an unstratified null, 0 against 42, refused; both as shipped, 0 against 6. At the 240-pair size the stratified-null-only reading is 163 against 163 - **exactly at the bar, no margin**. So the restricted randomisation on its own is not a reliable correction and the exclusion is what collapses the observed value; on the positive control the ordering reverses, rho staying at 1,000 either way while the stratification widens the null from 124 to 140. Both halves are load-bearing. What v2 estimates is the correlation over the **age-balanced subsample**, which is a different estimand from v1's, and that - not a moved reference distribution - is what makes a v1 and a v2 number incomparable. Wording corrected in all four places; no code changed, because the code was right and only the description of it was wrong. **The same pass found the commit had silently emptied one of its own tests.** `the_analysis_keys_on_the_entity_id_and_not_on_the_row_it_arrived_in` - added by D-100's mutation pass to close a survivor - was left on a birth-synchronised population while the new `NoInformativeStrata` refusal made exactly that population unscoreable, so both sides of its equality became the identical refusal `NoInformativeStrata { strata_total: 12, observations: 144 }` and it held whatever the analysis keyed on. **Measured: with every organism's counts read from the row opposite it - the precise defect the test names - the test still passed.** Rebuilt on triplet births with an in-group rotation, so the row order moves while every age stays put and the age-continuity check cannot stand in for the id key; two births rather than three is not enough, because a reversal inside a pair is an involution that a mirrored-row defect commutes with and cancels, measured. It now catches slot-keyed lookup, mirrored-row counts and next-row counts, and asserts the comparison is between two *computed* results so a future refusal cannot empty it again. **48 mutations were injected in this pass - 45 against the stratifier, the drop-both-sides rule, the refusals, the Fisher-Yates and the label plumbing, and 3 id-keying defects aimed at the emptied test; 8 survived, 4 of them real gaps from 3 causes and 4 tautologies, and after strengthening the 45-mutation matrix leaves only the tautologies.** Two of the closed four shared one cause: **every synthetic series in the module births organisms on a multiple of the window**, so every boundary age is a multiple of `W` and `age / W`, `(age + 1) / W`, `(age - W) / W` and `age / (W / 2)` all induce **the same partition** - and a stratum is only ever used as a partition. The pre-registered width and bin edge were therefore pinned by nothing; closed by `the_stratum_is_the_organisms_own_age_binned_at_the_window_width`, an off-grid-birth series in which the exclusion strands an odd 73 of 264 observations. The third: `distinct_distances`, and so `no_variance`, could be computed **before** the exclusion and nothing noticed, which publishes a world whose kept observations are all identical as a measured null instead of as undefined - D-079's distinction, one layer down; closed by `an_exclusion_that_takes_all_the_variance_leaves_the_world_undefined_not_null`, where one organism carries every varying observation and the single-label strata take exactly it. **The four surviving tautologies are recorded so nobody writes a test for them**: `pair_at` has already required its lookups to be `W` apart in age, so stratifying on the age at either end of the window is `age / W` minus or plus one and partitions identically (two of the four); the event/control counters in the strata index are read only through the symmetric `events > 0 && controls > 0`, so swapping them is a relabelling; and a missing post-window record falling back to the boundary record is unreachable behind `window_distance`'s empty-window refusal and the age check that precedes it. **One fragility, reported and not fixed**: `permutation_p95_milli_stratified` is a function of the sample *sequence*, not the sample set - reversing, rotating or re-sorting the input moved the p95 on 26 of 40 seeds - because Fisher-Yates consumes the same draws against a permuted member list. `permutation_p95_milli` has always had it. It is not reachable as nondeterminism, because `world_shift` assembles observations in a canonical order no input ordering can disturb, and changing it would move every recorded null; the `BTreeMap` walk is doing its job and the doc comment's "a function of the data and the seed alone" is now stated precisely instead. All six fixtures reproduce, Phase 11 at config `0xae34cd2b6f7a3e13` state `0x53b354bd94e82bcf` | Any change to the exclusion rule, the stratum width or the boundary pairing, each of which moves the numbers the three new tests pin; any caller of `permutation_p95_milli_stratified` that does not assemble its samples in a canonical order; and any comparison of a v1 rho with a v2 rho, which are correlations over different observation sets and not two readings of one quantity |
| D-107 | 2026-08-11 | Proposed technical baseline | **Shortening the plasticity conjunction: remove the dead rule value, do not merge the flag into the rule, and do not pick a default rule.** Reachability, not selection, is what Phase 11's null is about: a nonzero weight update needs five conditions to coincide on one expressed edge, each reached by a different one of seven point-mutation targets, and the measured joint is 9 of 684,370 alleles with independence predicting 12.0 - genuinely independent lottery digits, no interaction to exploit. Three ways to drop a digit were weighed. **(A1) Make `rule_id` 0 a live default rule.** Rejected as first stated, because choosing *which* rule sits at 0 authors which learning form evolution starts adjacent to, and ADR-0012's test is whether you can name the outcome a mechanism makes more likely. **(A2) Make an edge plastic iff `rule_id` names a live entry, retiring `EDGE_FLAG_PLASTIC` as a gate.** Attractive on paper - it deletes a redundancy rather than authoring a preference - and rejected on measured blast radius: sixteen files read the flag, and worse, **it breaks the one control C11.2 has.** The marker locus is matched to `EDGE_FLAG_PLASTIC` because `MARKER_FLAG_NEUTRAL` toggles on the same draw (target 1); `rule_id` is a five-way redraw on target 2 with 4/5 live, and the marker has no field of that shape. Re-pointing the control would mean adding a `rule_id`-shaped field to the marker, changing the ALG2 payload, to defend a criterion whose control is the only reason its null means anything. **A change that costs a control to buy a lottery digit is a bad trade.** **(A3, adopted) Remove the dead value from the rule id space** so every `rule_id` names a live rule and target 2 can no longer be spent on nothing. The objection to A1 does not apply, and the reason is measured rather than argued: `with_eta_zero_every_rule_in_the_registry_leaves_the_learned_state_alone` drives all five rules across three coefficient sets and four signal combinations and shows learned state and trace both stay at zero whenever `eta == 0`. **So the founder is inert under every rule id, and which rule it nominally carries hands it nothing** - the difference is only how many mutations the chain needs. It also corrects a misreading in the code's own comment: rule 0's early return is *not* what protects C11.4, `eta == 0` is, so removing the early return costs no guarantee. Consequence to carry: `StepKind::Static` disappears, and with it the 95.43 percent of the campaign's 1.1 billion "updates" that were early returns - which means applied-step pricing (the moat half) must charge on the learned state actually moving, not on a step being taken, or it would re-charge exactly what it set out to stop charging for | A measured interaction between the conditions, which would make them non-independent and change the arithmetic; a marker locus that gains a discrete multi-valued field for other reasons, which would make A2 cheap and worth revisiting; or evidence that removing `StepKind::Static` loses a diagnostic that was carrying weight |

| D-108 | 2026-08-15 | Proposed technical baseline | **ALIF format 5 reserves one config byte for `plasticity.live_rule_zero`, and three tests and one guard turned out to be written against "the current format" when they meant "format 4".** D-107's adopted option A3 has to be a runtime setting, not a compile-time registry change, because the 2x2 must run both arms against the same seeds on one build. But a new `PlasticityConfig` field must be encoded - not encoding it is D-065's defect in its sixth form, and `World::from_state` would refuse the restore outright rather than run quietly wrong - and `encode_config` is positional, so a naive addition shifts `worldmod` and `probe` by a byte and makes all 120 format-4 campaign artifacts undecodable. Hence a version bump. **The byte is appended after `probe` rather than filed with the plasticity block**, which buys one testable property in exchange for the grouping: the format-4 config body is a byte *prefix* of the format-5 body for the same world, stated as one assertion instead of a re-description of the layout. `SAVE_STATE_VERSION` does **not** move - format 5 adds a field and changes no existing field's meaning, and Phase 11 set the precedent by adding four config fields and a whole optional section without moving it. Both directions of a version/body mismatch fail closed on the *body* before the header's unchecksummed version word is consulted: a format-5 body read as 4 leaves one byte over and hits the trailing-bytes check, a format-4 body read as 5 runs out and hits `TruncatedSection`. **What the change actually exposed is a class of latent defect, found three more times in one file.** `SECTION_WORLDMOD` and `SECTION_ACTION_CENSUS` were guarded by `format < FORMAT_VERSION`, which is correct only while the introducing format is current and would, at format 5, have refused both sections in every format-4 file on disk - reported as an error naming the section rather than the comparison. Both now name `FORMAT_VERSION_4`. The same shape appeared in two tests that built their subject with `encode` (the current-format writer) while asserting something about format 4: `a_format_3_file_carrying_a_format_4_section_is_refused` began failing on the config byte before reaching the section it exists to test, and `a_disabled_world_encodes_the_payload_format_3_wrote` compared a format-5 file against a format-3 one. Both now use `encode_snapshot_format4` and mean what they say. And `FORMAT3_TO_FORMAT4`'s `to_format` was already `codec::FORMAT_VERSION`, so the name went stale the moment the constant moved; the transforms are renamed for their **source** (`FORMAT3_TO_CURRENT`, `FORMAT4_TO_CURRENT`) because every registered transform lands on the current format in one hop - `decode_snapshot_migrating` applies exactly one, so a format-3 file never becomes an intermediate format-4 file. Both declare `expected_loss: ""` and both are entitled to: a format-4 world ran with `live_rule_zero` false **by construction**, since no build that could write a format-4 file had a live rule 0 to switch on. `encode_snapshot_format4` refuses a state carrying the flag with a new `CodecError::FieldNotInFormat`, distinct from `SectionNotInFormat` because a config field has no tag and the refusal is write-side only. **The field is encoded but not behavioural, and `validate` refuses `true` in the gap** - on the same grounds it refuses a nonzero `lamarckian_fraction_q16`, that an accepted flag changing nothing hands a campaign an arm bit-identical to its own control and gets reported as a null. It is deliberately **not** in the config hash yet, because a hash difference claims a replay-lineage split and there is not one until the rule is remapped. No fixture moves: the default is false, the byte is zero, and the hash is untouched. **Mutation-tested by an agent that did not write the tests: 22 mutations, 19 caught, 3 survived**, and the survivors are the useful part. (i) Deleting `state.config.plasticity.live_rule_zero = false` from *either* transform changes nothing, because the reader already resolves the field the same way - so the line writes `false` over `false` and no test can see it. Setting it `true` **is** caught, which locates the limit exactly: the tests pin the transform's output and cannot pin which of two agreeing paths produced it. The comments now say so instead of crediting a test that cannot see it, and the same is true of the three format-3 assignments, so it is a property of the pattern rather than an oversight. (ii) **`an_adversarial_config_section_length_is_refused_...` earned no kill credit anywhere and would have passed with `MAX_SECTION_LEN` deleted.** Every value it forged was still refused, each by a *later* rung of the ladder in `decode_payload` - `checked_add` overflow, the `body_end + 4` truncation check, or the section CRC - so the bound it exists to defend was load-bearing only by inspection. This is standing rule 2's failure mode one level up from the rule's own wording: not a bit flip absorbed by a CRC, but a bound whose removal is masked by a bound underneath it. Making `MAX_SECTION_LEN` the only guard standing would need a gibibyte of fixture; asserting the *exact* error costs nothing and discriminates as well, since without the bound those values report `TruncatedSection`. Re-verified by re-applying the mutation: the test now fails with `left: Some(TruncatedSection), right: Some(ValueOutOfRange("section length"))`. Two below-bound values are pinned to the rung that should catch them, so "everything returns `ValueOutOfRange`" is not a passing answer. Measured coverage fact worth keeping: `phase12_format4.rs` is entirely **blind** to the section-guard defect - only `format5.rs` catches it, because a migration test built on a bare Phase 2 world carries neither guarded section | The next config-block growth, which must take a format bump rather than an append (see the 2026-08-15 open question on the retained format-3 reader); or `SAVE_STATE_VERSION` moving for an unrelated reason, which would need `SAVE_STATE_VERSION_4` to be pinned as its own constant the way `SAVE_STATE_VERSION_3` is |

| D-109 | 2026-08-15 | Measured result | **Cross-platform determinism is now measured rather than assumed: every pinned fixture reproduces on x86_64 Linux having been generated on arm64 macOS.** `AGENTS.md` forbids claiming cross-platform determinism without evidence, and until now there was none - every `scripts/verify-*.sh` run had happened on one machine, and each script compares two runs of *the same build*, which is evidence about process independence and says nothing about architecture. With `genesis-engine` (Ubuntu 24.04, x86_64, rustc 1.97.1) as a second host, all six scripts pass there against constants generated on macOS arm64: Phase 0 and Phase 1 `0x1e3158a26afd3b39`, Phase 2 `0xff9dfcff5dffbf42`, Phase 9 config `0x9abc0cd47914127f` / state `0x5f0c4e95e4f5170f`, Phase 11 config `0xae34cd2b6f7a3e13` / state `0x53b354bd94e82bcf` over 10^6 ticks, plus Phase 5's event-log and campaign-manifest checks. The full workspace suite is 682/682 on both hosts. **What this is evidence for, stated narrowly:** two architectures, one compiler version, one `--release` profile. It is not a claim about other compiler versions, other optimisation levels, or any platform not listed. The mechanisms it exercises are the ones the determinism rules were written to protect - fixed-point accumulation over a lifetime, per-node float summation pinned to ascending `homology_id`, and named RNG substreams - and a float-order or endianness bug in any of them would have surfaced as a differing checksum rather than as a differing test count. **Why it matters beyond the claim:** the 10^6-tick Phase 11 trace is a single-organism fixed-point plasticity accumulation, which is the most float-order-sensitive artifact in the repository; that it lands on the same 64-bit checksum on two architectures is the strongest single piece of evidence Rule 7 and the `homology_id` summation order are doing what they were written to do | A third architecture, a compiler-version bump, or any change to the release profile's `lto`/`codegen-units`, each of which would need re-measuring rather than inheriting this result |

| D-110 | 2026-08-15 | Proposed technical baseline | **The `live_rule_zero` remap narrows the rule id space rather than redirecting it, and the deciding evidence is the commissioned review rather than an argument about taste.** See ADR-0027. The backlog specified "remap the rule ONCE in `compile_with_budget`", and building it exposed that a compile-time remap alone cannot deliver what D-107's option A3 asked for: `structmut.rs:633` draws uniformly over `PLASTICITY_RULE_COUNT` = 5 and `genome2.rs:257` reduces mod 5 at expression time, so a remap confined to compile sees five equiprobable inputs and four live rules, and **every such map makes one rule twice as likely as the others**. That is A1's objection - the one D-107 rejected the option for - wearing a different hat. The doubled rule under the obvious map `r -> (r % 4) + 1` is rule 1, plain Hebbian, and the lifetime-learning review section 6.3 is explicit that a plain unbounded Hebbian rule "should not be a production default", rating it "strongly supported as a baseline; unsupported as the sole production rule" - so that option authors a preference for the one rule the review names as the wrong default and the one with the worst runaway-weight failure mode. Adopted instead: the flag makes the effective rule count 4 for both the draw and the expression reduction, with `compile_with_budget` mapping `r` to live rule `r + 1`; four live rules at 25 percent each, no rule introduced, removed, reordered or preferred. **Two things checking this turned up that the obvious worry gets backwards.** (i) Genome validity does *not* become config-dependent: `normalized` reduces rather than rejects, deliberately, because "a `rule_id` that had to name a registry entry in order to decode would make most rule-id mutations lethal for a reason that has nothing to do with learning" - so under a narrower modulus every bit pattern still names some rule and no genome becomes invalid in either arm. (ii) What *does* change is that the arms no longer share a genotype-to-phenotype map: a stored `rule_id = 7` expresses as rule 2 with the flag clear and rule 3 with it set, so a genome may not be transplanted between arms and read as the same organism. The 2x2 compares seed-matched worlds rather than genotypes, so its design is unaffected, but any future cross-arm genome comparison must be refused rather than interpreted. RNG streams are untouched - one draw at the same position in the same named stream, only the modulus on its value changes - so no fixture can move for that reason. Cost carried knowingly: `normalized()` has eight call sites across `structmut.rs`, `genome2.rs` and `develop.rs`, all of which the effective count must reach, which is wider than the one-call-site instruction and is what the uniformity buys | A measured failure of founder exchangeability across rule ids, which would make a uniform draw itself a choice about where evolution starts and reopen A1's objection against this option too; a fifth live rule; or evidence that the doubled-Hebbian bias is immaterial to the 2x2, which would make the narrow implementation preferable on blast radius. **Implemented 2026-08-16, and implementing it found a consequence the ADR had not anticipated: the founder lands on Hebbian.** The founder stores `rule_id = 0`; `0 % LIVE_RULE_COUNT` is 0; so the founder's allele names `LIVE_RULE_BASE`, which is plain Hebbian - and since the census measures 93.46 percent of compiled plastic edges still carrying the founder's value, the standing population under the chain is about **92 percent Hebbian**, measured. This is D-107's objection to option A1 arriving at the *population* level after being avoided at the *draw* level, and it is uncomfortable in a specific way: option (b) was rejected for giving plain Hebbian 40 percent, and the adopted option gives it 92 percent of standing alleles. It is not a reason to reverse, because under **any** scheme where every `rule_id` names a live rule the founder's id names one and every untouched allele inherits it - the concentration comes from a low mutation rate meeting a founder-dominated population, not from which mapping was chosen, and the alternatives are declining A3 or correlating rule identity with lineage. Two obligations follow instead: the 2x2's reachability endpoint is invariant to it (it counts completions, not which rule completed - part of why a payoff endpoint would have been the wrong choice), and the **compiled rule histogram plus `plasticity_saturations_total` must be reported in every arm**, because this engine's Hebbian is bounded by `LEARN_LIMIT_Q16` and the saturation counter is the measurement of how hard that clamp is working. Without both, an arm that destabilised because nearly everything was Hebbian would read as "learning is harmful". Found by a test failing for the right reason, not by review; pinned by `a_flag_on_config_compiles_only_live_rules_and_draws_them_uniformly` so it cannot be rediscovered |

| D-111 | 2026-08-16 | Proposed technical baseline | **The moat prices on learned state moving, not on `StepKind::Applied`, and the difference is the whole 2x2.** The campaign pre-registration first specified "price on `StepKind::Applied`, not on `plastic_edges.len()`", and building it showed that would have silently destroyed the design. `Applied`'s own definition is "the rule form ran and the learned state was rewritten (**possibly to the same value**)", and the only non-`Applied` kinds are the rule-0 early return and an unreachable refusal - so under `live_rule_zero`, which removes rule 0, every plastic edge is `Applied`, `applied == plastic_edges.len()`, and a moat priced on it charges exactly what today's engine charges. **Arm B would have been arm C wearing a second flag**, and the 2x2 would have had three distinct arms and a duplicate. D-107 named this failure in writing - "applied-step pricing must charge on the learned state actually moving, not on a step being taken, or it would re-charge exactly what it set out to stop charging for" - and the pre-registration's first draft reintroduced it by naming the wrong mechanism. Corrected in the pre-registration **before** the code was written and before any campaign ran, which is what that document reserves the right to do. The adopted basis is `outcome.state != LearnedState { learned_q16, trace_q16 }` as it stood before the step, covering both the learned weight and the eligibility trace. **Measured rather than argued**: applying the applied-step mistake as a mutation and running `the_moat_still_charges_less_than_the_flat_price_when_the_chain_is_on` gives "Charged 191172 against a flat 191172" - bit-for-bit identical, the moat a complete no-op. The correct basis charges 0 in the same world. `plasticity_updates_applied`, from the counter split, remains the right diagnostic; it is simply not the right price. The flag is `plasticity.price_moved_edges_only`, hashed only when set and only when the section is enabled, so no fixture moves | The price basis becoming a rate rather than a predicate, which would make a zero rate and a disabled moat indistinguishable; or a rule whose `step` can move the trace without moving the weight in a way that makes "the learned state" ambiguous |
| D-112 | 2026-08-16 | Proposed technical baseline | **ALIF format 6 reserves a second config byte, and the recurring test trap now has a single parameterised guard.** `plasticity.price_moved_edges_only` needs the same treatment `live_rule_zero` got in D-108: `encode_config` is positional and unconditional, so one more field is one more format, one more retained reader and writer, one more registered migration. That cost does not amortise, and the format-5 note now says so explicitly - a third config field should expect it, or propose a self-describing config block in an ADR that explains how an absent trailing field avoids being "altered meaning on load". **What the second bump exposed is that the trap D-108 recorded twice has a third instance and a general shape**: a test named for a format that builds its subject with the *current-format* writer, which is the same thing only until the next bump. D-108 caught two in `phase12_format4.rs`; format 6 broke `the_format_4_config_body_is_the_format_5_body_without_its_last_byte` the same way. Rather than fix it in place a fourth time, `format6.rs` carries `each_adjacent_format_adds_exactly_one_config_byte`, which walks a table of retained writers and asserts, for every adjacent pair, that the newer config body is the older one plus exactly one byte and that the older is a byte prefix of it. **Adding format 7 means adding one row to that table**, and a format that does not extend its predecessor by exactly one byte names itself in the failure. Format 3 is deliberately outside the chain: it differs from format 4 by a section and a logical-state version, not by a config byte, and `phase12_format4.rs` owns that comparison. `SAVE_STATE_VERSION` stays 2 for the same reason it stayed at format 5 | A config-block change that is not a single appended byte, which the chain test would refuse and which would need its own reasoning; or an accepted ADR for a self-describing config block, which would retire the chain entirely |

| D-113 | 2026-08-16 | Measured result | **C11.7's checkpoint-stall half is measured through `AsyncCheckpointer`, and producing it found two harness defects that had been silently shortening every Phase 11 benchmark record.** The criterion asks for checkpoint stall "at both tiers with realistic evolved plasticity levels"; what had been reported was synchronous encode/decode/restore time on the tick thread, which the plan itself calls a substitution rather than an answer. Measured with A5.5's methodology so the two records are comparable - same tiers, same 200-tick interval, checkpoint ticks sampled separately from all ticks because at 0.5 percent of the sample a p95 over all ticks cannot see a stall by construction. Against a 100 ms budget fixed by `dt_ms`: async checkpoint-tick p50 is **3.41 ms at tier 500 and 13.94 ms at tier 2000**, against synchronous 21.22 and 52.07; worst async tick 14.86 ms is 15 percent of budget, worst synchronous 61.78 ms is 62 percent. `async_refused` is 0 in every arm. **Plasticity does not measurably move the stall**: seeded against off is +2.5 percent at tier 500 and **-2.4 percent** at tier 2000, a sign change, so it is reported as noise rather than as a small cost. `evolved` carries 0 plastic edges at tier 500 and 8 in the whole population at tier 2000 - the phase's named most-likely failure appearing again - so `seeded` is the arm that bounds the answer, and `plastic_edges_per_organism_milli` prints beside every timing so a small stall cannot be read as "sparse learned state is cheap" when it means "nothing was plastic". **Allocation in the learn path is zero, measured rather than read off `plasticity::step`**: a whole `step` legitimately allocates, so the claim is tested by whether the count *moves* across the 0/1/2 plastic-edge sweep in a sterile immortal world - 12,100 / 12,082 / 12,057 at tier 500 and 12,149 / 12,105 / 12,078 at tier 2000, so **3,125,484 applied updates added zero allocations**. Verified by injecting one `Vec::with_capacity(1)` per plastic edge per tick: 606,236 allocations against 12,100, caught. **The two harness defects.** (i) `scripts/run-phase11-benchmarks.sh` never invoked the kernel-side benchmark and its comment still said that target "is not written yet" long after `bench_phase11.rs` existed - so every record it produced had no `learn` lines and nothing said so. (ii) The timed kernel benchmarks need `--test-threads 1`, and serialised, cargo prints `test <name> ... ` **with no trailing newline**, so each test's first `println!` lands on that line and `grep '^PHASE11-BENCH'` drops it: **every benchmark test silently lost its first measurement**, which here was the zero-plastic-edge baseline every other arm is compared against. Parallel runs do not do this, which is why the other six phase scripts have never hit it. Extraction is now `grep -o`, and the script fails if the record is not exactly 25 measurements - a silently short record being precisely the failure that had just occurred | A third tier, a change to `dt_ms` (the budget is the tick interval, not a chosen target), or a checkpoint interval short enough that the single-slot queue starts refusing, which would make `async_refused` a number rather than a zero |
| D-114 | 2026-08-16 | Proposed technical baseline | **The set of channels a schema-2 lineage binds is fixed at its founder, and has been since Phase 9.** Found while designing the artifact half's action channels, by asking how an evolved organism would come to request `pick_up`: point mutation on an `IoBinding` locus perturbs only `gain` (`structmut.rs:706`), insertion adds edges and nodes and never bindings, duplication copies a binding's `channel_id` unchanged, and `minimal_founder` binds exactly two channels - 1 `energy_fraction` and 101 `turn` (`structmut.rs:1057-1150`). So in every schema-2 world - the Phase 9 and 11 fixtures, the Phase 9 campaign, both Phase 11 campaigns - every organism reads one sense and writes one action, and every other channel sits at its neutral value's side of its threshold: `eat` fires always (threshold -0.5), `throttle` at half, `follow`/`avoid`/`mate`/`attack` at whatever their thresholds make of zero. `registry.rs`'s module doc says "an organism binds any subset it likes"; it can bind any subset its founder bound. **This is trap 16 of the evidence-traps memory again** - a documented capability with no write path - and had the artifact half added five channels to it, C12.1's null would have been mechanically guaranteed and reported as ecology. Adopted (ADR-0028 section 8): a `bind` structural operator at rate `genome2.mutation.binding_q16`, zero by default and hashed only when nonzero on D-014's field-granularity precedent, inserting one `IoBinding` for a uniformly drawn node and a uniformly drawn channel of the world's registry. Not enabled by default, so no fixture moves; common-mode across all four Phase 12 conditions. **Not decided here: whether Phases 9-11 should be re-run with it on.** Recorded as an open question, with the note that every structural-evolution result to date is a result about topology over a two-channel interface | Any claim that a schema-2 result generalises to a richer sensorimotor interface; the Phase 9-11 re-run question |
| D-115 | 2026-08-16 | Proposed technical baseline | **The artifact half's design is ADR-0028, and every departure from the commissioned review is written there beside its reason.** Thirteen read passes over the review and the code preceded it. Decisions in one line each: the five verbs are kept because the pre-registered criteria name them, each realised as the review's corresponding primitive under grid constraints, with `combine` as review 8.5's own fallback (a non-guaranteed, energy-costing bond attempt) rather than the guaranteed verb review 18.5 forbids; the per-material `affordances` bitset is replaced by predicates over physical quantities (`carryable` is mass against the asker's capacity, `consumable` is energy > 0, `blocks_movement` is a mass threshold, everything is strikable and combinable subject to caps); perception exposes `object_heft` and `object_hardness`, not `object_material` or `object_composite_depth`, plus `carried_load`; the composite is a new object owning retained constituents (spec) rather than a transient assembly handle (review 6.7), because the criteria are stated over composites and their depth and exact restoration on fracture needs the records; the fracture draw is keyed on the target because damage aggregates across strikers (review 13.7); every attempted action costs energy; **the channel registry is world-gated** - `CHANNEL_REGISTRY_VERSION` stays 1, the eleven object channels are version 2, a world's version is 2 iff its artifact section is enabled, the ALG2 stamp is the smallest version covering a genome's bindings and the reader accepts both - because bumping the constant would have moved two fixtures and made every schema-2 genome on disk undecodable (`genome2.rs:816`), which is the opposite of what `registry.rs:11-14` promises. Numbers fixed before code: channels 17-22 and 113-117 (109-112 stay unallocated), `RngSystem::Artifact = 13` and `MaterialYield = 14`, `SECTION_OBJECTS = 15` guarded on format 7, **ALIF format 7** for the appended config block (the chain test's one-byte rule becomes a per-row byte delta, the "own reasoning" D-112 said a non-one-byte extension would need), `SAVE_STATE_VERSION` stays 2, `next_entity_id` keeps its name, checksum tag `lifesim-object-state-v1` appended **last** rather than in the Rule 8 table's position, event schema 6 with tags 14+. Three spec conflicts resolved: `experiment-config-schema.md`'s two sections `materials`/`artifacts` become one; `long-horizon-soak.md`'s "world total not capped" yields to C12.7's object-count cap; the spec's "Phase 13 fixture" is D-096's four | The Phase 12 cost measurement reopening D2; a C12.1 null diagnosed as "wrong action shape", which would motivate review 19.2's bonding experiment |
| D-116 | 2026-08-16 | Proposed technical baseline | **`artifact.max_composition_breadth` cannot bind and is recorded as vestigial rather than left to look enforced.** A `combine` joins exactly two objects (the lowest-id held and the nearest free target), so every composite's direct constituent count is 2; validation refuses `max_composition_breadth < 2` (a world that could not combine at all would be condition D wearing another name); the `BreadthCap` refusal path therefore exists and is reachable by no valid configuration. Found while extending `every_cap_rejects_counts_and_events_when_driven` to the two caps it had not driven (carry capacity, which binds and is now asserted; breadth, which does not). Options: (a) reinterpret breadth as the count of leaf constituents in the whole tree, which would give the cap meaning at depth 3-4 but is a behaviour change under Rule 9 (new `lifesim-artifact` policy version, new config hash for every artifact world, the trace fixture re-pinned) while the pre-registered campaign is running on the current kernel; (b) remove the field, which is a format-7 layout change for nothing; (c) keep the field and the refusal path, state that the cap is degenerate in the spec, the ADR and C12.7's status, and assert in the test that a busy combining world under a valid config never records a breadth refusal. **Chosen: (c) now; (a) is the recorded follow-up if a non-binary combine ever exists, and it must go through the policy version.** C12.7's claim is correspondingly 'depth, occupancy, carry capacity, held count and world object count reject, count and event when driven; breadth is a field that validation bounds and no world can press against'. Evidence-trap entry: the cap test's first extension asserted zero pick-ups at a ten-milli capacity and was refused - seven pick-ups, every one a carcass eaten down to a few milli, which does fit; the assertion became 'whatever is held fits', which is the claim the cap makes. |
| D-117 | 2026-08-16 | Recorded result | **The Phase 12 artifact-half confirmatory campaign returned three measured nulls with controls: C12.1 0 of 30 (bar 20), C12.2 7 of 30 (a, bar 15) and 11 of 30 (b, bar 20), C12.3 5 of 30 (bar 20, N fixed in the pre-registration), condition D zero by construction.** Campaign `0x36c2d68a9a0869da`, 120 worlds x 60,000 ticks, kernel c56a7c7, analysis `lifesim-artifact-analysis-v1`; findings in `experiments/results/phase12-artifact-confirmatory-findings.txt` beside the manifest and the report. No threshold moved after the data. The number that explains the rest: under A pick_up/place/combine were requested 48,137,380 times in 3.77 billion organism-ticks and succeeded 41,703 times (one in 1,154; 11 ppm succeeded, 12,755 ppm fired), refused overwhelmingly as NothingHeld (45.1M) and NoTarget (14.1M); the fire rate did not differ between A and C (16/30 positive, p 0.427). **Unlike Phase 11's null (D-099) this is not a reachability null**: the census read from every final snapshot shows living genomes bound to each of the five actions in 30/30 worlds per arm, the pick_up-and-place conjunction in 28/30, 24% of living organisms binding at least one action, 603,412 bind operations applied per arm - D-114's operator did its job. The null is that firing did not pay enough to be shaped in 60,000 ticks at reach 2 m, hold cap 1 and these costs; the follow-up knobs are the costs, the rate and the reach, each its own pre-registered campaign. What was found on the way: the log decoder's operator range stopped one short of `OP_BINDING` and refused every log with a rejected bind (reader-side, fixed, sample extended); depth-two composites arose by chance in 15/30 worlds (60 in all) and rose over the run in 5 - a real small positive that the criterion's frequency does not accept; carcass objects returned 923 million milli of energy to the population under A, an ecological effect the plan did not name. |
| D-118 | 2026-08-16 | Recorded confound, fix deferred | **The condition-C (`artifact.inert`) arm skipped carcass consumption as well as the five verbs, so its ecology differed from A's beyond 'actions have no effect'.** `artifact_phase`'s inert early return sits before the consumption step; carcass objects existed under C (293,560) but were never eaten and 1,953 of 1,960 million milli of their energy decayed, against 923 million milli consumed under A. Demography shows it: starvation deaths +6% and damage deaths -42% under C. It does not touch the verdicts (C12.2 and C12.3 do not read C; C12.1's 0/30 is a factor of a thousand between successes and fires) and it is recorded here rather than repaired after the fact: the pre-registration is closed and the campaign is on disk. The fix - inert skips exactly the five verbs and lets consumption, decay, exposure and carry accounting run - is a behaviour change for inert worlds under Rule 9, so it goes in with a new `lifesim-artifact` policy version, a re-pinned trace fixture (old and new hashes recorded), and before the next artifact campaign is pre-registered. Open question added to `docs/21-open-questions.md`. |
| D-119 | 2026-09-01 | Proposed technical baseline | **`lifesim-artifact-v2`: the inert arm skips exactly the five verbs, and the Phase 12 trace fixture is re-pinned.** The D-118 fix, landed before any next artifact campaign as its entry required. `artifact_phase`'s inert gate no longer returns early: the five verbs are charged and counted with no effect exactly as before, and consumption of object energy, the exposure and carry accounting, and the commit pass now run in both arms (decay always ran in its own phase). Behaviour changes for inert worlds only, but the policy string is hashed in the config hash and in the `lifesim-object-state-v1` checksum tag for every artifact world, so under Rule 9 every artifact world starts a new replay lineage and the fixture moves: **config `0xc64259e739b525d4` -> `0x21405a5c0591ceeb`, state `0x853d257398a2718c` -> `0x24defb6052eb9d42`** (`scripts/verify-phase12-determinism.sh`, all four clauses green; the Phase 1, 2, 9 and 11 fixtures are untouched, verified by the same script and `verify-phase11-determinism.sh`). The load-bearing test is `an_inert_world_still_eats_its_carcasses_and_records_exposure`: a carcass injected into an inert world is eaten and ledgered, exposure accrues from a free organism-created object, and the verbs still confer nothing. Standing rule 1 discharged by re-injecting the exact v1 defect (the early return) and watching that test fail - and the pre-existing inert test **passes with the mutation in place**, which is why it could never have caught D-118. An **independent mutation pass by an agent that did not write the tests** ran 15 mutations of its own design against the inert/verb/consumption/observation routing: 9 caught, 5 survived, and the survivors were the finding - the routing was defended and the *pricing* was not (a verb could be counted uncharged, billed at another verb's price, or counted without its intent, and three of the five verbs' inert arms were executed by no test at all, proven by a panic probe that never fired). Closed by `the_inert_charge_block_counts_each_verb_once_and_bills_it_at_its_own_price`: two seed-matched inert worlds, all-verbs against no-verbs, pin each counter to one fire per tick and zero respectively, and pin the difference of their `spent_milli` ledgers to the exact per-tick bill at distinct per-verb prices - each of the three real survivor classes was re-injected and dies against it. Two survivors are tautologies, recorded so nobody writes a test for them: 6b's `is_free` guard cannot be false while the cell index only ever holds free objects (kept as the cheap local assertion for the day an insert changes that invariant), and gating section 6 on `!inert` is unobservable because `landed` is only ever pushed in the non-inert branch. Two test-scenario facts found on the way, recorded for the next scenario author: `ObjectRecord::simple`'s last parameter is `parent_id`, not `creator_id` (trap 36's neighbour - the exposure pass counts only `creator_id != 0`, which `simple` never sets), and a carcass small enough to be eaten in one tick is destroyed in that tick, before the observation pass reads the destroyed flags, so an exposure assertion needs an inedible object | Any next artifact campaign's pre-registration, which must state its arms against v2; and any reading of the 2026-08-16 campaign's C arm, which stays under D-118's caveat - the campaign is not re-run |
| D-120 | 2026-09-01 | Recorded result | **The Phase 11 reachability 2x2 ran (2026-08-16, commit c2141ec) and was never entered in this log; recorded here without alteration.** Campaign `0x8efbcf9694206dc6`, 48 worlds (4 arms x 12 matched seeds), 60,000 ticks, built at 974cb7d, run on genesis-engine, pre-registered in `experiments/phase11-reachability-preregistration.md` before the run. On the pre-registered expressed-edge measure and 10-of-12 bar: **the chain (ADR-0027 `live_rule_zero`) meets its bar - C 10 of 12, B 11 of 12, effect 13.0x against a 10.7x prediction made in advance - and the moat fails its bar** (M 4 of 12, total below N) while doing exactly what it was built to do (plasticity cost 2,459x cheaper). `static` steps are zero in both chain arms; nonzero learned edges 448 under C against 8 under N (56x); the predicted ~92% Hebbian concentration measured at 92.5/94.7%, with C the only arm hitting the learn clamp (733 saturations) - the reporting obligation D-110 imposed earned itself. The findings file also records a pre-registration defect worth carrying: its section 3 named two outcome measures, and the allele-level one **cannot see the chain by construction** (the remap lives in `compile_with_budget`; `normalized` still reduces mod 5), so the next pre-registration must check that its outcome measure can observe its factor. NOT supported, stated there and repeated here: any claim that plasticity is *useful* (payoff was deliberately not the endpoint), any moat reachability claim, any interaction claim. Full record: `experiments/results/phase11-reachability-findings.txt` | A Phase 13 or ladder campaign that wants within-lifetime learning assemblable, which should carry `plasticity.live_rule_zero = true` and cite this result; and any payoff claim, which needs its own campaign under the C11.1 control-boundary correction first |
| D-121 | 2026-09-01 | Proposed technical baseline | **Phase 13 proceeds without Phase 18, decided from measurement rather than assumption.** The roadmap schedules intra-world parallelism "after 8, before 13" on the argument that Phase 13's criteria need population most and a null under the suspected blocker would be confounded. Checked against what parallelism can actually change: **campaign worlds are single-threaded by rule** (ADR-0023; ADR-0026 "campaign worlds stay single-threaded and remain the basis for every claim"), so Phase 18 cannot raise the population of any world that decides a criterion - it raises the flagship ceiling only, and no flagship world exists or is gated on. What bounds campaign population is wall-clock per world, and the measured record says the current populations are far from that bound: the Phase 11 confirmatory ran 120 worlds x 60,000 ticks in 4,512.9 s at median populations 2,716-5,964 (D-105); the Phase 12 campaign's A arm carried ~2,100 mean organisms per world (3.77e9 organism-ticks / 30 worlds / 60,000 ticks, D-117); the 2x2 ran 48 worlds in 4,651.7 s on the VM (D-120). A Phase 13 pre-registration that wants 10,000-organism worlds pays ~5x wall clock - hours, not a redesign - and that choice belongs to the pre-registration, not to a parallelism phase. ADR-0026's own population concern (cumulative culture may need populations above the Tasmanian ~4,000 loss threshold) is therefore a **campaign config question first**, and the Phase 13 pre-registration must state its population tier and cite this entry. Phase 18 stays scheduled, unmoved, for when a flagship world is wanted or a campaign design exceeds the wall-clock budget single-threaded | A Phase 13 pre-registration whose power analysis demands per-world populations that do not fit the wall-clock budget single-threaded; or a decision to run a flagship world |
| D-122 | 2026-09-01 | Proposed technical baseline | **Phase 13's mechanism is built: perception cues, the costly signal field, the condition gates, and the gated observational rule (ADR-0029), with its fixture pinned.** Five commissioned reviews were consulted before the design was written and are cited in the ADR by their own section numbers. Registry version 3 (nine cues per neighbour slot at 23..58, `signal_in` 59..62, `signal_emit` 118..121), offered only with `social.enabled`, which requires `artifact.enabled` - the version scheme is a total order because a genome's stamp is the smallest covering version. RNG streams `Signal = 11` and `Perception = 12` allocated (the two values held as spare capacity since D-036); event schema 7 (tags 24, 25); ALIF format 8 with the retained format-7 writer/reader and registered migration; checksum tag `lifesim-social-state-v1` appended last; `SECTION_SOCIAL = 16`. Rule 5 sits in a `RULE_SPACE` one wider than the base registry rather than raising `RULE_COUNT` - **the first attempt bumped `RULE_REGISTRY_VERSION` and the Phase 11 clean-process trace failed within the hour**, so the constant stays 1 and the world's *offered* version (2 under condition P) is what the config hashes, the channel registry's precedent exactly. Conditions realized as config gates: `perception_enabled` (C), `signal_enabled` (B), `scramble_delivery` (D, the emission recentred on a drawn receiver with the bill byte-identical), `observational_enabled` (P/S, the S arm verified by `rule5_updates_total == 0` rather than by the flag). Fixture `scripts/verify-phase13-determinism.sh`: config `0x252199db7099e9a5`, state `0x5861f0fc8ab02957`, 8,000 ticks, twelve non-vacuity fields refused at zero, three condition arms replaying as distinct lineages, and the Phase 1/2/9/12 fixtures preserved (Phase 11 by its own script). Every spec deviation is recorded in the ADR beside its reason (the carried cue is a load fraction, visible phenotype is scale and health, motion is the committed speed fraction, the object delta is caused-by, cost has no separate range factor, attenuation is `1-(d/range)^2`). **The behavioral criteria C13.1-C13.10 are NOT measured**: per the ladder, the transmission criteria are expected to read null until a learning campaign under levers 1-3 succeeds, and the campaign must carry its own pre-registration | The Phase 13 campaign's pre-registration, which must fix populations, corruption levels, K, the SESOI and the claim wording before any run; and the sim-analysis reachability census, which C13.1's interpretation needs the way D-117's did |
| D-123 | 2026-09-01 | Proposed technical baseline | **The independent mutation pass on the Phase 13 kernel ran 28 mutations of its own design: 10 caught by cargo, 6 more only by the fixture script, and 12 by nothing - and the 12 are the finding, now closed except one.** The pass (an agent that wrote none of the tests) verified the routing and the gates were defended and found the suite's scenarios structurally too gentle: every world was sterile and immortal inside its window (so `retain`/`push_organism` never ran in cargo), every scripted amplitude was exactly one whole (so the D-094 remainder never moved), only one cue of one slot was ever read (so the K-nearest sort, tie-break, truncation order and cue stride were pinned by nothing), the top `signal_in` channel was bound by no genome, and the condition arms asserted only replay-and-distinctness (so the corruption draw's keying and the scramble draw's self-exclusion were verified by nothing). Closed by six new tests plus two verify-script pins: analytic decay-then-add (the empirical two-world form is **provably blind** - add-then-decay equals a stamp rescaling, `(c+S)k = ck + Sk`, clamp included, so only an absolute expectation discriminates); simultaneous-emitter sum-before-clamp; the exact rational bill at a gain-0.9 amplitude; a breeding-and-dying social world (lockstep in cargo); five-neighbour slot ordering with an exact-tie id break; per-channel gather including channel 62; per-channel and per-receiver corruption keying at the sense surface (corrupted cues are scratch, so **no state checksum can see keying** unless a genome binds the channel); and pinned state checksums for the scramble (`0x82c08143180116dc`) and corruption (`0x440c31960b6cef8c`) arms. Every closure was verified by re-injecting the pass's own mutation and watching the new test fail. **Recorded residual**: the corruption draw keyed on storage index instead of organism ID (the pass's M10) survives everything - index and ID coincide in every cheap scenario, and post-compaction cues are unbound scratch; the honest closure is a `signal_in` binding in the trace scripts, taken when the trace next re-pins for a real reason rather than moving three fresh constants for it. **Two non-tests recorded so nobody writes them**: the rule-5 counter's `Applied` guard is dead (rule 5 always applies; the guard survives any mutation), and the chain-only modulus is a near-tautology whose real risk - a leak into the gated rule through a flag-clear save or a seeded founder - is pinned by `the_chain_alone_can_never_reach_the_observational_rule` instead | The trace's next re-pin, which should add a `signal_in` binding to the scripts and close M10; and any new social scenario, which should breed, die, and bind more than one cue |
| D-124 | 2026-09-01 | Proposed technical baseline | **Phase 13's Stage A and Stage B closed in one day, and the confirmatory campaign is running under a pre-registration locked before it started.** Stage A: the C13.1 arrival detector (`lifesim-arrival-detector-v1` - naive cohort from the event log joined fail-closed to the spatial series in ascending-entity-ID order, the patch footprint as the kernel's own CellSquare through the new read-only `World::patch_centre_for_epoch`, censoring counted never imputed), the C13.2 fidelity detector (`lifesim-fidelity-v1` - policy mixes from the action census, exposure from the spatial series, and the genetic control as **exact Q32 pedigree kinship reconstructed from `PairedBirth` events**, chosen over final-snapshot genotype distance because the pedigree covers every organism ever born rather than the survivors), the C13.3 tradition detector (`lifesim-tradition-v1` - the era-and-tradition spec's four requirements including the mandatory genotype-matched control, census-derived mixes recorded as the as-built deviation), the C13.8-C13.10 classifiers (`lifesim-communities-v1` - repeated-association chains against a **proximity-matched null** that permutes identities among home-quadrat peers so home ranges cannot masquerade as communities, plus the within/between aggression ledger with opportunity-pair denominators), the `lifesim social` and `social-contrast` commands, and `PairedResult.reaching_absolute_directed` (the relative SESOI form is None at a zero control - for an expected-null baseline that silently excludes most pairs, D-120's blindness in statistical clothing). Every detector carries Gate E scripted ground truth, and every one was self-mutation-checked with re-injection; the survivors found and closed: a control borrowed across kinship bins, a dropped concentration factor, a drop-the-clamp cell mapping. Stage B: the pilot (16 worlds, 33.3 min, `experiments/phase13-social-pilot.campaign`) measured the tier affordable (240 worlds ~8.3 h), cohorts plentiful at epochs 10-25, **reachability decisively non-null** (hearers 446-2,854 and speakers 301-1,788 per world; 3.2-10.9 million priced emissions per A world - binding drift at the Phase 12 rate populates the social channels without any selection story), the pilot's own A-vs-C contrast null (mean -5 milli, CI [-14,+1]), and one instrument lesson now locked into the reading notes: **`rule5_alleles` is blind under the chain's live-space renumbering** (stored 4 names rule 5; the pilot showed zero stored-5 alleles beside up to 42 expressed observational edges), so the expressed-edge count is the interpretive number. The pre-registration (`experiments/phase13-social-preregistration.md`) locks epochs {10,15,20,25}, the mean naive arrival fraction, seed-paired between-arm contrasts only (D-100's age-free form), SESOI 20 milli absolute directed, the 20/30 bar on BOTH A-vs-C and A-vs-D, analysis seed 0x1373, the power statement, and the expected null - committed at 37983b7 before the first confirmatory world ran. The C13.11 soak (10^6 ticks, arm A) runs on the VM in parallel | The confirmatory's findings file, which must be read only through the locked rules; the C13.7 classifier, which needs a phenotype instrument the artifacts do not yet carry (an event or census column - its own increment with schema discipline); and the C13.8-C13.10 addendum, which must be pre-registered before those classifiers touch this campaign's artifacts |
| D-125 | 2026-09-01 | Proposed technical baseline | **The independent mutation pass over the day's five analysis modules ran 63 mutations of its own design: 32 caught, 5 proven equivalent, 26 survived - closed by 22 verified tests - and it found three production defects the author's own Gate E never touched.** The pass (an opus agent in an isolated worktree that wrote none of the code) confirmed the boundaries the self-checks had pinned (alive-set replay edges, Chebyshev-vs-Euclidean, kinship halving, the concentration factor) and found the gaps concentrated where the self-checks were structurally gentle: rounding and median conventions, join edge ticks, the CLI contrast's arithmetic (8 of 9 contrast mutations survived - the original CLI test checked shape, not identity; the closure asserts the contrast IS the arithmetic of the per-world lines against the unmutated per-epoch reports). The three defects, all fixed with the closures kept: (1) **the arrival median took the upper central value** while demography and paired take the lower and arrival's own doc claims their convention - a C13.1 reported quantity; (2) **tradition findings were deduplicated by (quadrat, variant) across incommensurable label spaces** - variant labels are local to one endpoint pair's joint clustering, so the same variant relabels between endpoints and the key double-counts; dedup is now per quadrat, which is what the per-world criterion reads anyway; (3) **`spatial_positions_by_id` kept two hand-synchronized copies of the alive-set predicate and joined with a silently truncating zip** - the fail-closed check used one predicate, the join the other, so the moment they disagreed positions would be misassigned with no refusal; it now has one predicate and its own length check. A fourth finding was a comment claiming a load-bearing property the labelling provably does not depend on (union direction); reworded. Integration also forced the fidelity reduction to be bounded honestly for campaign density (>10 min for 16 pilot worlds before; 4:36 after): exposure from the bucketed pair scan at the window's opening sample, kinship only after the stride cap, per-bin control queues where a member conflict peeks and only use consumes (the pass's weighting test caught the first shared-cursor design starving later exposed pairs), a counted control budget and window stride - every bound echoed in the output, none silent | Any campaign reading of fidelity numbers, which must quote the caps beside them; and the next independent pass, which should target the communities and recognition modules that landed after this one forked |
| D-126 | 2026-09-02 | Proposed technical baseline | **The Phase 13 confirmatory returned the pre-registered expected null on both required contrasts, and the censuses prove it is a transmission null, not a reachability null.** 240 worlds (8 arms x 30 matched seeds 13001..13030), 0 failed, 0 extinct; findings at `experiments/results/phase13-social-confirmatory-findings.txt` with the per-world reports and a recomputable reduction script beside it; raw run archived at `runs/phase13-social-confirmatory-0x5a0ee0ccb8d48df7` (60 GB). C13.1: A-vs-C 0/30 reaching the SESOI (mean -4 milli, CI [-8,-1]), A-vs-D 0/30 (mean -1, CI [-5,+1]) - and every A world individually carries hearers (min 331), speakers (min 57) and a naive cohort (min 634) at the horizon with ~7M priced emissions per world, so the channel is populated and paid for; what it carries does not help. Three results the pre-registration could not state in advance: (1) **speaker depletion under billing** - arms where emission is priced hold ~520-550 median speakers against ~1,400 where the same genes cost nothing (B/C); emission cost is selecting the transmitter out of the population before transmission can pay, the "signals become noise" risk realized as silence rather than lies, and the named lever for the next social iteration; (2) **the C13.2 F-curve is flat AND the no-channel arms sit on it** - exposed-vs-control similarity deltas of +13..+16 milli at corruption 0/8192/16384/32768 with B at +19 and C at +17, so the exposure delta is spatial assortment finer than the kinship bins, not transmission, and any future use of `lifesim-fidelity-v1` must subtract a no-channel baseline; (3) **the C13.3 tradition detector's control arm convicts it** - C fired 4/12 against a locked bar of zero (findings roughly uniform across all eight arms), so its positives are inheritance-plus-spatial-structure false positives everywhere and the era-and-tradition spec needs a sharper control (within-lineage placebo or quadrat permutation) before its verdicts mean anything; recorded as a methods finding, not tuned post hoc. C13.4 landed on the plan's third outcome (A-vs-S 0/30, mean -1, CI [-2,0]: both fail, scaffolding controlled). The conflict addendum's readings all returned their expected nulls with arm symmetry (C13.8 10/30 A vs 10/30 C; C13.9 6/30, p=1.000, mean -234 milli with C's boundary factor *higher*; C13.10 advantage ratios 1.85:1 vs 1.81:1, coalition rates 48 vs 47 milli). The S ablation verified at scale (zero expressed rule-5 organisms in all 30 S worlds). C13.11 passed (ledger exact over 10^6 ticks). C13.5/C13.6 reported by their own wording: undefined / rate zero | The C13.7 recognition reading (60 schema-8 worlds, campaign 0x33099bb271e99cde, reduction running when this was recorded) completes the findings file; any social-channel redesign starts from the emission-pricing lever and must re-pre-register; the fidelity and tradition detector fixes are analysis-side and need no schema bump |

## Entry Template

Record: ID, date, status, context, options, choice, evidence, performance impact, operational/security impact, compatibility effect, owner, and explicit revisit condition. Link the associated ADR when the decision has broad or durable consequences.

## Rule-Change Ledger

For each simulation-policy change, record old config/schema hash, new hash, affected formulas/traits, expected effect, migration/replay policy, validation tests, benchmark/scenario result, and whether existing worlds branch or remain on legacy rules.

# Copy to /etc/genesis-engine/runtime.env with mode 0600, root:genesis.
# Generate distinct, high-entropy values in the approved secret manager.
LIFESIM_OBSERVER_TOKEN=replace-with-observer-token
LIFESIM_ADMIN_TOKEN=replace-with-admin-token
